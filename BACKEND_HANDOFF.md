# Trabis — Backend Handoff

مستند مختصر لأي باك اند هيشتغل على نقل Trabis (الاسم القديم SmartAc، والـ keys في localStorage لسه بادئة بـ `smartac_`) من نسخة الـ prototype (localStorage بس، من غير سيرفر) لنسخة حقيقية على سيرفر (v2.0). اقرأ ده الأول قبل ما تبص على الكود.

> **لو إنت بتطبّق تعديلات سبتمبر 2026 على نسخة شغّالة على سيرفر (كود مختلف عن الريبو ده): اقرأ [`CHANGES_FOR_BACKEND.md`](CHANGES_FOR_BACKEND.md) بدل ده.** المستند ده عن الهجرة من الـ prototype للسيرفر؛ التاني عن التعديلات نفسها.

> **آخر تحديث: سبتمبر 2026.** الأقسام من «فجوتان في الـ Schema» لحد «الـ OCR» اتضافت بعد شغل على الواجهة كشف حاجات بتأثر عليكم مباشرة — لو إنت الباك اند، دول أهم حاجة في المستند.

## الوضع الحالي (مهم تفهمه قبل أي حاجة)

- `index.html` ده كل التطبيق (HTML/CSS/JS في ملف واحد، من غير build step ولا framework). بيتفتح مباشرة في المتصفح، ومنشور دلوقتي على Vercel كـ static file بس — يعني Vercel بتقدّم الملف زي ما هو، مفيش سيرفر شغال فعليًا.
- **مفيش backend ولا database حقيقية دلوقتي.** كل بيانات المستخدم (معاملات، عملاء، فواتير، موظفين، إلخ) متخزنة في `localStorage` و`IndexedDB` **في متصفح المستخدم نفسه**، مفيش أي مزامنة بين الأجهزة، ومفيش حماية حقيقية (كلمة السر متخزنة بـ `btoa` بس، مش تشفير فعلي).
- ده معناه: أي جهاز/متصفح جديد = بيانات فاضية من الصفر. لو المستخدم مسح الـ cache، بياناته راحت.
- تفاصيل أكتر عن البنية الحالية موجودة في [`README.md`](README.md).

## المطلوب فعليًا

نقل التطبيق لمعمارية حقيقية: **Auth + Database + File Storage على سيرفر**، والكود الحالي (اللي دلوقتي بيقرأ/يكتب على `localStorage` مباشرة) يتحول يكلم الـ backend ده بدل كده.

## اللي اتعمل فيه شغل قبل كده (خلفية Supabase الموجودة)

اتعمل تحضير مسبق باستخدام **Supabase** (Postgres + Auth + Storage managed) على منطقة **Frankfurt (eu-central-1)** لأسباب الـ DSGVO (البيانات تفضل جوا الاتحاد الأوروبي):

- **الـ Schema كامل**: 21 جدول في [`supabase/migrations/20260712120000_initial_schema.sql`](supabase/migrations/20260712120000_initial_schema.sql) — بيغطي كل فيتشرات التطبيق (transactions, clients, suppliers, employees + work hours + documents, invoices صادرة/واردة, fleet + maintenance, assets, inventory, notes, appointments, debts, recurring/fixed income, installments, UVA approvals, receipts).
- **Row Level Security (RLS) مفعّل على كل جدول**: كل يوزر يشوف/يعدّل بياناته هو بس (`auth.uid() = user_id`)، ونفس المبدأ على Supabase Storage.
- **Storage buckets**: `receipts` و`employee-documents` جاهزين (بالـ RLS بتاعتهم كمان).
- **Trigger أوتوماتيك**: أي يوزر جديد يعمل signup، صف `profiles` بتاعه بيتعمل تلقائيًا.
- **Supabase Auth (email + password) مفعّل** بالفعل على مستوى المشروع.
- **نشر أوتوماتيك للـ migrations**: أي تعديل في `supabase/migrations/**` بيتنشر تلقائي عند الـ push لـ `main` عن طريق [`.github/workflows/supabase-migrations.yml`](.github/workflows/supabase-migrations.yml) (GitHub Actions + Supabase CLI).
- الـ schema ده **Postgres عادي** — مش حاجة خاصة بـ Supabase تحديدًا، فحتى لو قررتوا متستخدموش Supabase، تقدروا تاخدوا نفس ملف الـ SQL وتشغّلوه على أي Postgres server عادي.

## قرار مطلوب منكم: تستخدموا Supabase ولا سيرفركم الخاص؟

| | استخدام Supabase (الموجود أصلاً) | سيرفر خاص بيكم |
|---|---|---|
| Auth | جاهز ومفعّل، بس محتاج ربط بالكود | تبنوه من الصفر (أو تستخدموا حاجة زي Auth.js/Passport) |
| Database + RLS | جاهزين، Schema كامل ومختبر | تاخدوا نفس ملف الـ SQL كـ نقطة بداية، لكن الـ RLS بالصيغة دي مرتبطة بـ `auth.uid()` بتاعة Supabase Auth تحديدًا — لو غيرتوا نظام الـ Auth هتحتاجوا تراجعوا منطق الحماية |
| File Storage | Buckets جاهزة | تحتاجوا حل تخزين ملفات بديل (S3 مثلاً) |
| النشر/الاستضافة | Supabase managed، مفيش سيرفر تديروه | لازم تديروا السيرفر بنفسكم |
| السرعة للإطلاق | الأسرع — البنية التحتية شغالة أصلاً | وقت إضافي لإعادة بناء نفس الحاجات |

**التوصية**: لو مفيش سبب قوي (مثلاً عندكم عقد/بنية تحتية مفروضة عليكم)، الأسرع إنكم تستخدموا Supabase الموجود بدل ما تبنوا من الصفر — كل الشغل الصعب (الـ Schema والـ RLS) خلصان ومتأكد منه.

## فجوتان في الـ Schema لازم تتسدّوا

جدول `profiles` فيه كل إعدادات الحساب تقريبًا، **ما عدا اتنين**:

| الإعداد | موجود في الكود | موجود في الـ Schema |
|---|---|---|
| `state.uvaInterval` (شهري/ربعي/سنوي) | ✅ | ❌ |
| `state.vatBasis` (`'ist'` / `'soll'`) | ✅ | ❌ |

التانية اتضافت في سبتمبر 2026 (اقرأ القسم اللي تحت). الاتنين عايزين عمودين نصيين في `profiles`:

```sql
alter table profiles
  add column if not exists uva_interval text default 'quarter',
  add column if not exists vat_basis    text default 'ist';
```

وأي `migration` جديدة بتتنشر أوتوماتيك من `.github/workflows/supabase-migrations.yml` عند الـ push على `main`.

## أساس احتساب الضريبة (`vat_basis`) — مهم للـ UVA

الشركات النمساوية بتشتغل بواحد من اتنين، مش الاتنين:

- **`ist`** (Ist-Besteuerung) — الفاتورة الصادرة تبقى دخل **يوم ما تتحصّل**. ده الافتراضي والسلوك القديم.
- **`soll`** (Soll-Besteuerung) — تبقى دخل **بتاريخ إصدارها**، اتحصّلت أو لأ.

في الكود اتقسّمت الدالة لاتنين عشان الازدواج يبقى مستحيل:

- `bookInvoiceIncome(inv, date)` — بتسجّل الدخل بس، ومحميّة بـ `inv.incomeTxId`
- `handleInvoicePaid(inv)` — بتسجّل الدخل **و** تطلع إيصال الدفع

في `soll` الفاتورة بتتسجّل وقت الإصدار، وإيصال الدفع بيفضل مستني الدفع الحقيقي. ولما تتعلّم مدفوعة بعدين، الحارس `incomeTxId` بيمنع التسجيل التاني.

**لما تنقلوا ده للسيرفر: خلّوا القاعدة دي في مكان واحد.** لو الـ client سجّل الدخل والـ server سجّله كمان (أو trigger)، الفاتورة هتتعدّ مرتين وأرقام الـ UVA هتطلع مضخّمة. الأنضف إن التسجيل يبقى `trigger`/`function` على السيرفر والـ client يبعت الفاتورة بس.

## تلات مخاطر هجرة اتكشفت وإحنا بنشتغل

### 1. التسجيل التلقائي للقواعد المتكررة — أخطر واحدة

`checkRecurringDue()` بتحوّل كل قاعدة في `recurring` و`fixed_income` ورواتب `employees` لـ **معاملة حقيقية** أول ما ييجي يومها، وبتختمها بـ `last_recorded = 'YYYY-MM'`.

دلوقتي بتشتغل **على الـ client عند كل فتح للتطبيق**. لما تبقى البيانات على السيرفر، ده بيبقى **race condition حقيقي**: جهازين يفتحوا التطبيق في نفس الوقت، الاتنين يقروا `last_recorded != '2026-09'`، والاتنين يعملوا `insert` — فالراتب يتسجّل مرتين.

الحل مش اختياري:

- إمّا تنقل المنطق لـ **cron job على السيرفر** (Supabase `pg_cron` أو Edge Function مجدولة) يشتغل مرة لكل حساب
- أو تخليه `insert` واحد ذرّي بشرط `where last_recorded is distinct from $month`، مع **unique constraint** يمنع التكرار أصلًا

في سابقة جاهزة في الـ schema بتاعكم: `uva_freigaben` فيها `unique (user_id, period_key)`. اعملوا نفس الحاجة هنا، مثلًا `unique (recurring_id, period_key)` على جدول تسجيلات.

> ده مش كلام نظري: نفس المنطق طلّع **ازدواج فعلي في الواجهة** أثناء الشغل — الراتب ظهر مرتين والصافي السنوي طلع غلط بفرق كبير. اتصلح على الـ client، بس السبب الجذري (إن القاعدة بتولّد معاملة) هيفضل موجود على السيرفر.

### 2. تسرّب بيانات بين الحسابات — ما تنقلوهوش معاكم

`logout()` بيصفّر **٩ حقول بس** من الـ `state` (الـ object فيه ~٤٥). وبعدين لو المستخدم سجّل حساب جديد في نفس التبويب، `save()` بيكتب **كل** الـ `state` تحت مفاتيح اليوزر الجديد — فالحساب الجديد بيرث موردين وفواتير وملاحظات و**بيانات بنك (IBAN)** بتاعة اليوزر القديم.

على جهاز واحد الضرر محدود. **على السيرفر بيبقى أخطر بكتير**: البيانات المتسرّبة هتتزامن على كل أجهزة اليوزر الجديد.

خلّوا `logout()` يرجّع الـ `state` لقيمه الابتدائية بالكامل، وخلّوا أي كتابة تتأكد إن اليوزر اللي بتكتب له هو نفسه اللي اتحمّلت بياناته (في متغيّر `_loadedDataUser` موجود أصلًا ومستخدم جزئيًا).

### 3. المرفقات مش مقسّمة لكل يوزر

مخازن IndexedDB (`smartac_receipts`, `smartac_empdocs`, `smartac_fleetdocs`) **مخزن واحد للجهاز كله**، و`receiptGet(id)` بتجيب الملف بالـ id من غير ما تتأكد من صاحبه.

الـ Storage buckets عندكم فيها RLS جاهزة، فالنقطة دي بتتصلح لوحدها وقت الهجرة — **بشرط** إن الـ path يبقى مبدوء بـ `user_id` وما تعتمدوش على الـ id لوحده.

## الشاشة الرئيسية بقت شهرية — ليه ده يهمّك

اتعمل إعادة تصميم للهوم (سبتمبر 2026، متنفّذة ومدموجة):

- أزرار الفترات الستة (اليوم/الأسبوع/الشهر/الربع/السنة/مخصص) اتشالت، ومكانها **سنة + ١٢ شهر**
- `state.period` بقى ثابت على `'month'`، وزادت `state.year` و`state.month` كمرساة
- الشهر اتقسم **دخل / مصاريف**، وكل واحد فيه «دفعات ثابتة» منفصلة عن الحركات
- مرفقات الشهر بتتعرض كشريط صور

**الأثر على الـ backend**: التطبيق بقى بيسأل عن **شهر واحد في المرة**، مش عن كل البيانات. ده فرصة مهمة — اعملوا الـ queries بـ range على `date`، وحطّوا index مركّب:

```sql
create index if not exists transactions_user_date_idx on transactions(user_id, date);
```

`state.year` و`state.month` **حالة واجهة بس** — ما تعملولهمش أعمدة.

## الـ OCR / الـ "AI-Mitarbeiter" — محتاج السيرفر قبله

الفكرة: المستخدم يصوّر فاتورة (أو يرفع صورة/PDF)، والتطبيق يقرأها ويطلّع **اقتراح** جاهز — مورّد، مبلغ، نسبة ضريبة، فئة، تاريخ استحقاق — والمستخدم يوافق.

**ليه مستحيل دلوقتي**: مفتاح الـ API ما ينفعش يعيش في `index.html` — الملف بيتقدّم كما هو من Vercel وأي حد يفتح View Source ياخده. فالفيتشر دي **بتفرض** وجود سيرفر، وده أقوى مبرر تجاري لمرحلة الـ Cloud.

الحد الأدنى: **Edge Function واحدة** تمسك المفتاح وتعمل proxy.

المفصل جاهز في الكود من بدري:

```js
// index.html — extractInvoiceData(file)
// "Once a backend AI endpoint exists, replace the body of this function with
//  a call to it — the return shape {date, brutto, rate} stays the same."
```

دلوقتي بتشتغل بـ Tesseract محلي + regex. لما ييجي الـ backend، استبدلوا جسم الدالة بنداء واحد — باقي الكود (التصوير/المراجعة/الحفظ) ما بيتغيرش.

نقاط لازم تتحسم قبل التنفيذ:

- **مستويات الصلاحية**: قراءة وتصنيف واقتراح = تلقائي · التسجيل في الدفاتر = **محتاج موافقة** · التحويلات والحذف والإجراءات الضريبية = ممنوع بدون تأكيد صريح. الواجهة لازم تقول "اقتراح — لسه ما اتسجّل" ومتقولش "اتسجّل" قبل ضغطة المستخدم.
- **نسبة الضريبة تفضل اقتراح دايمًا.** ١٠٪ بدل ٢٠٪ بتمشي لبيان UVA بيقدّمه المحاسب — دي عواقب ضريبية، مش خطأ عرض.
- **DSGVO**: إرسال فواتير فيها أسماء موردين ومبالغ لمزوّد AI = **Auftragsverarbeitung**، محتاج AVV/DPA وإفصاح للمستخدم. المشروع متحطّ في Frankfurt عمدًا لنفس السبب — تأكدوا إن الاستدلال نفسه بيتنفّذ جوه الاتحاد الأوروبي (في APIs بتوفّر بارامتر لتحديد منطقة الاستدلال وبترجّع فعليًا وين اتنفّذ).
- **خزّنوا اقتراح الـ AI جنب تصحيح المستخدم** — ده أثر تدقيق، وكمان بيانات مجانية لتحسين الـ prompt.

## اللي لسه ما اتعملش (ده شغلكم الأساسي)

الكود في `index.html` **لسه بيستخدم `localStorage` بالكامل** — مفيش أي اتصال فعلي بـ Supabase (أو أي backend) لسه. التفاصيل الكاملة (checklist) موجودة في [`TODO.md`](TODO.md) تحت قسم **"المرحلة 3 — تحويل الـ App للـ Cloud (v2.0)"**، وأهمها:

- استبدال نظام تسجيل الدخول الحالي (`btoa` + localStorage) بـ Supabase Auth
- تحويل دالتي `loadUserData()` و`save()` (نقطة الدخول لكل قراءة/كتابة بيانات في التطبيق) من localStorage لـ Supabase
- تحويل كل الـ CRUD operations (معاملات، عملاء، موردين، فواتير، موظفين، ديون، أسطول، أصول، ملاحظات، مواعيد...) واحدة واحدة
- رفع الملفات (إيصالات، مستندات موظفين، أرشيف) فعليًا على Storage بدل IndexedDB المحلي
- Loading states + error handling على كل عملية
- اختبار multi-device حقيقي (نفس الحساب من جهازين مختلفين)
- إضافة عمودي `uva_interval` و`vat_basis` لجدول `profiles` (فوق)
- نقل التسجيل التلقائي للقواعد المتكررة من الـ client للسيرفر، بـ unique constraint (فوق)
- إصلاح `logout()` قبل الهجرة، مش بعدها (فوق)
- Edge Function للـ OCR تمسك مفتاح الـ API (فوق)

## نقطة مهمة: تسجيل الدخول بجوجل

اتسأل عن ده قبل كده — مش ممكن يتضاف كـ patch بسيط على النظام الحالي، لازم يجي **بعد** خطوة "استبدال الـ Auth" فوق، لأن Google OAuth محتاج domain حقيقي + session سيرفر، مش localStorage. لو استخدمتوا Supabase Auth، إضافة Google بعد كده هتبقى: عمل OAuth Client في Google Cloud Console + تفعيل الـ provider في إعدادات Supabase Auth + زرار "Sign in with Google" بينادي `signInWithOAuth`.

## طلب Access

لو هتستخدموا Supabase الموجود، هتحتاجوا Access على المشروع من صاحبه (Frankfurt project) — كلموه يضيفكوا كـ team member من Supabase Dashboard.
