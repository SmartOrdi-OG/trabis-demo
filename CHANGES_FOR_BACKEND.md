# تغييرات سبتمبر 2026 — مواصفة للتطبيق على النسخة اللي على السيرفر

**دي مواصفة وظيفية، مش patch.** (السيرفر مكتوب Java — في قسم مخصص لده في الآخر: [ملاحظات لنسخة Java](#ملاحظات-لنسخة-java).)

**التوضيح:** النسخة اللي على السيرفر كود مختلف عن `index.html` اللي في الريبو ده، فنسخ الأسطر مش هينفع. اللي تحت هو **المنطق والقواعد** المطلوب تطبيقها، مستقلة عن أي framework.

كل حاجة هنا اتنفّذت واتجرّبت فعليًا على نسخة الديمو. مرجع بصري شغّال (تقدر تضغط فيه):

- شاشة الشهر: https://claude.ai/artifact/9uwcVTtJ4Wb64r6AVys5ki
- شاشة السنوات (نسخة أقدم من الفكرة): https://claude.ai/code/artifact/c6d58ed3-4825-4bf2-adb2-f6f7b0575c12

---

## ملخص

| # | التغيير | نوعه |
|---|---|---|
| 1 | فلتر الهوم بقى **سنة + شهر** بدل ست فترات | واجهة + منطق فلترة |
| 2 | الشهر اتقسم **دخل / مصاريف**، وكل واحد فيه «ثابت» منفصل | واجهة + تجميع |
| 3 | مرفقات الشهر بتتعرض **كشريط صور** | واجهة |
| 4 | إعداد حساب جديد: **أساس احتساب الضريبة** (Soll/Ist) | منطق محاسبي + عمود DB |
| 5 | **إصلاح ازدواج احتساب** — اتأكد منه في نسختك | bug fix |

**البند 5 هو الأهم.** لو نسختك فيها نفس المنطق، الأرقام عندك غلط دلوقتي.

---

## 1. فلتر الهوم: سنة + شهر

### اللي اتشال
الست أزرار: اليوم / الأسبوع / الشهر / الربع / السنة / مخصص.

المشكلة فيها إنها ما بتقولش **أنهي** شهر. «الشهر» يعني الشهر الجاري دايمًا، ومفيش طريقة تشوف يوليو وانت في سبتمبر.

### اللي مكانها
شريط فيه **زرار سنة + ١٢ زرار شهر مرقّمين ١–١٢**. الشهر المختار بيطلع أطول وأوضح (شكل تبويب ملف).

### القواعد

**حالة الفلتر** بقت قيمتين بس:

```
selectedYear   (رقم، الافتراضي = السنة الحالية)
selectedMonth  (0–11، الافتراضي = الشهر الحالي)
```

دول **حالة واجهة** — ما بتتخزنش في قاعدة البيانات ولا بتتزامن بين الأجهزة. كل فتح للتطبيق بيبدأ من الشهر الحالي.

**شرط انتماء المعاملة للفلتر**:

```
tx.date.year == selectedYear AND tx.date.month == selectedMonth
```

**قائمة السنين** تتولّد من البيانات نفسها، مش لستة ثابتة:

```
كل سنة فيها معاملة واحدة على الأقل، + السنة الحالية
مرتّبة تصاعدي
```

حساب جديد فاضي = زرار واحد للسنة الحالية.

**المقارنة بالفترة السابقة** (لو عندك «مقارنة بالشهر الماضي»): الشهر اللي قبل **المختار**، مش اللي قبل الحالي.

```
prevMonth = selectedMonth == 0 ? 11 : selectedMonth - 1
prevYear  = selectedMonth == 0 ? selectedYear - 1 : selectedYear
```

**التسميات**: أي مكان كان بيكتب «هذا الشهر / This Month / Diesen Monat» لازم يكتب اسم الشهر الفعلي + السنة — مثلًا `Juli 2026`. ده ينطبق على العناوين، والتصدير، والـ PDF، والمشاركة.

**الفلتر المخصص** (مدى تواريخ) **يتشال من الهوم**. لو عندك تقارير أو UVA فيهم اختيار فترة، سيبهم زي ما هم — دول منطق ضريبي مش فلتر عرض.

### حالة حدّية مهمة

لما المستخدم يصوّر/يرفع فاتورة وتاريخها **برّا الشهر المفتوح**، التطبيق لازم **ينقل الشاشة لشهر الفاتورة** — مش يفرض فلتر مخصص ولا يخفيها.

السبب: المستخدم بيصوّر فاتورة يوليو وهو واقف في سبتمبر. لو حفظتها وسيبته في سبتمبر، هيفتكر إنها ضاعت.

في نسخة الديمو بتظهر رسالة صغيرة بتقول: *«اتسجّلت في يوليو — لأن تاريخ الفاتورة 12.07، مش الشهر اللي كنت فيه»*.

---

## 2. الشهر اتقسم: دخل / مصاريف

### الشكل

تبويبين فوق القائمة. التبويب المختار بيعرض:

```
الصافي (Netto)   الضريبة (MwSt)        ← مجاميع الجانب ده في الشهر ده

▸ ثابت · مستحق        [مجموع]          ← مجموعة أولى
  الاسم        اليوم        المبلغ
  ...
▸ الباقي              [مجموع]          ← مجموعة تانية
  الاسم        اليوم        المبلغ
  ...

الصافي السنوي · 2026            [رقم]  ← تحت الكل
```

### مصادر المجموعة «الثابتة»

| التبويب | المصدر |
|---|---|
| مصاريف | القواعد المتكررة (`recurring`) + رواتب الموظفين |
| دخل | الدخل الثابت (`fixed_income`) |

كل صف بيعرض **يوم نزوله في الشهر المختار**، مش تاريخ إنشاء القاعدة.

### ⚠️ القاعدة اللي من غيرها الأرقام تطلع غلط

**القاعدة الثابتة تتعرض في المجموعة دي بس طالما لسه ما اتسجّلتش كمعاملة للشهر المعروض.**

ليه: التطبيق (عندنا وغالبًا عندك) بيحوّل كل قاعدة متكررة لمعاملة حقيقية أول ما ييجي يومها، وبيختمها بـ `last_recorded = 'YYYY-MM'`. لو عرضت القاعدة **و** المعاملة اللي هي ولّدتها، الاتنين بيتعدّوا.

المنطق:

```
monthKey = "YYYY-MM" للشهر المعروض
nowKey   = "YYYY-MM" للشهر الحالي

لو monthKey < nowKey          → ما تعرضش ولا قاعدة (شهر قديم: كله اتسجّل وقته)
غير كده                        → اعرض القاعدة بس لو rule.last_recorded != monthKey
```

**شهور المستقبل**: القواعد بتتعرض كلها (مجدولة، لسه ما نزلتش).

### الصافي السنوي

رقم واحد تحت القائمة، نطاقه **السنة كلها** مش الشهر — عشان كده متحطّ في برواز منفصل ومكتوب جنبه السنة.

```
الصافي السنوي =
    مجموع صافي (netto) معاملات السنة: دخل − مصاريف
  + (لو السنة = السنة الحالية) صافي القواعد الثابتة اللي لسه ما اتسجّلتش
```

الشق التاني مهم: الشهور اللي فاتت موجودة أصلًا في المعاملات، فلو ضفت القواعد كمان هتعدّها مرتين.

### البحث

لما المستخدم يكتب في البحث، بدّل العرض المجمّع بـ **قائمة مسطّحة للنتايج** عبر كل الشهور. لما يفضّي البحث، ارجع للتجميع.

---

## 3. شريط مرفقات الشهر

مرفقات الشهر بتتعرض **صور مصغّرة فوق القائمة**، مش مخبّية جوه كل معاملة.

- المصدر: كل معاملة في الشهر المعروض **من نوع التبويب المختار** وعندها مرفق
- **صورة** → عرض مصغّر حقيقي
- **PDF** → أيقونة مستند (ما تحاولش تعمل preview)
- الضغط بيفتح نفس العارض الموجود عندك
- تحت كل صورة: اسم المعاملة + مبلغها

لو الشهر مفيهوش مرفقات، الشريط كله يختفي — ما تسيبش مساحة فاضية.

**ملاحظة تنفيذ**: لو بتعمل object URLs للصور، حرّرها قبل كل إعادة رسم وإلا الذاكرة بتتراكم مع التنقّل بين الشهور.

### قرار تصميم

في المحاكاة الصور كانت **عمود جنب الأرقام**. في التنفيذ بقت **شريط أفقي فوقها**.

السبب: الهوم صفحة طويلة بتتمرّر، فلوحين جنب بعض جوّاها بيعملوا تمرير جوه تمرير — تجربة سيئة على الموبايل. لو شاشتك بارتفاع ثابت، العمود الجانبي أحسن.

---

## 4. إعداد جديد: أساس احتساب الضريبة

### المطلوب

إعداد **على مستوى الحساب** بقيمتين:

| القيمة | المعنى | الفاتورة الصادرة تبقى دخل |
|---|---|---|
| `ist` | Ist-Besteuerung | **يوم ما تتحصّل** |
| `soll` | Soll-Besteuerung | **بتاريخ إصدارها**، اتحصّلت أو لأ |

**الافتراضي `ist`** — ده السلوك القديم، فمفيش حساب قايم بتتغيّر أرقامه لوحدها.

الاتنين قانونيين في النمسا. الشركة مسجّلة على واحد منهم تحديدًا، فالمستخدم لازم يختار اللي متفق عليه مع محاسبه.

### مكانه في الواجهة

جنب اختيار فترة الـ UVA مباشرة — لأنه بيأثر على نفس الأرقام. وتحته سطر بيشرح المختار بالكلام مش بالمصطلح:

> «الفاتورة الصادرة بتتحسب دخل يوم ما تتحصّل.»
> «الفاتورة الصادرة بتتحسب دخل بتاريخ إصدارها، اتحصّلت أو لأ.»

### قاعدة البيانات

عمود نصي في جدول إعدادات الحساب:

```sql
alter table profiles add column if not exists vat_basis text default 'ist';
```

> وانت بتعملها: جدول `profiles` كمان ناقصه `uva_interval` (التطبيق بيحفظه بس مفيش عمود ليه). فرصة تضيف الاتنين مع بعض.

### ⚠️ التسجيل لازم يحصل في مكان واحد بالظبط

دي أخطر نقطة في المستند كله.

المنطق اتقسم لاتنين:

```
bookInvoiceIncome(invoice, date)
    ├─ لو الفاتورة متسجّلة قبل كده (عندها incomeTxId) → اخرج فورًا
    ├─ اعمل معاملة دخل بتاريخ date
    └─ خزّن المعرّف على الفاتورة (incomeTxId)

handleInvoicePaid(invoice)
    ├─ bookInvoiceIncome(invoice, today)   ← بتشتغل بس لو لسه ما اتسجّلتش
    └─ اطلع إيصال الدفع
```

والاستخدام:

| اللحظة | `ist` | `soll` |
|---|---|---|
| إنشاء الفاتورة | لا شيء | `bookInvoiceIncome(inv, inv.issueDate)` |
| تعليمها مدفوعة | `handleInvoicePaid(inv)` | `handleInvoicePaid(inv)` — التسجيل بيتخطّى، الإيصال بس بيطلع |

**النقطتين المهمتين**:

1. **إيصال الدفع بيفضل مستني الدفع الحقيقي** حتى في `soll`. الدخل يتسجّل وقت الإصدار، لكن ما ينفعش تطلع إيصال دفع لفاتورة ما اتدفعتش.

2. **الحارس (`incomeTxId`) هو اللي بيمنع الازدواج.** من غيره، الفاتورة في وضع `soll` هتتسجّل مرة عند الإصدار ومرة تانية عند التحصيل.

لو عندك trigger على قاعدة البيانات بيعمل نفس الحاجة، **شيل واحد منهم**. التسجيل لازم يعيش في طبقة واحدة — الـ client أو السيرفر، مش الاتنين.

### التبديل مش رجعي

تغيير الإعداد **ينطبق على الفواتير الجديدة بس**. ما تعملش ترحيل رجعي للفواتير القديمة: ده بيغيّر شهور مقفولة واعتمادات UVA موقّعة، وده قرار محاسبي مش تقني.

خلّي رسالة بتقول ده وقت التبديل.

---

## 5. ⚠️ ازدواج احتساب — اتأكد منه في نسختك

**ده مش تحسين، ده إصلاح غلط في الأرقام.**

### الأعراض

في نسخة الديمو، شهر سبتمبر كان بيعرض راتب الموظف **مرتين**:

```
دفعات ثابتة      راتب · Stefan Moser    28.09    −2,600.00
مصاريف أخرى      Stefan Moser - راتب    01.09    −2,600.00
```

والصافي السنوي طلع **−16,780.83** والصح **+7,144.17** — فرق ~24 ألف يورو.

### السبب

التطبيق بيحوّل القواعد المتكررة (والرواتب والدخل الثابت) لمعاملات حقيقية أول ما ييجي يومها. وبعدين الواجهة كانت بتعرض **القاعدة والمعاملة** مع بعض.

### إزاي تتأكد إن عندك نفس المشكلة

1. افتح شهر قديم عدّى يوم استحقاق الرواتب فيه
2. دوّر على اسم الموظف أو القاعدة في القائمة
3. لو ظاهر مرتين (مرة كقاعدة ومرة كمعاملة) → عندك نفس الغلط

الحل موصوف في البند 2 فوق.

### وحاجة تانية لو نسختك على سيرفر فعلًا

التسجيل التلقائي ده لو بيشتغل **على الـ client**، فمع المزامنة بين الأجهزة بيبقى **race condition**: جهازين يفتحوا التطبيق في نفس اللحظة، الاتنين يقروا `last_recorded` القديمة، والاتنين يسجّلوا.

الحل: نقله لـ job على السيرفر، أو `insert` ذرّي بشرط `where last_recorded is distinct from $month` **مع unique constraint** يمنع التكرار من الأساس.

في سابقة في الـ schema بتاعنا: `uva_freigaben` فيها `unique (user_id, period_key)`.

---

## 6. تفاصيل صغيرة

**الأرقام في العربي**: علامة `+` / `−` كانت بتظهر **بعد** الرقم (`€419.00-` بدل `-€419.00`) لأن النص لاتيني جوه حاوية RTL. لو تطبيقك بيدعم العربي، خلّي ترتيب الرقم نفسه LTR جوه الاتجاه العام.

**الصفر محايد**: شهر فاضي كان بيعرض `+0.00` بالأخضر — كأنه ربح. الصفر لا أخضر ولا أحمر.

**الشهور الفاضية**: اعرض سطر «لسه ما بدأ» بدل عناوين مجموعات فاضية.

**أسماء لاتينية في واجهة عربية**: كانت بتتقص من الأول (`…umarkt Wien Süd` بدل `Baumarkt Wien S…`). خلّي كل اسم ياخد اتجاهه من محتواه.

---

## ترتيب التنفيذ المقترح

1. **البند 5** (الازدواج) — إصلاح أرقام، مستقل عن كل حاجة تانية
2. **البند 1** (التنقّل الشهري) — أساس اللي بعده
3. **البند 2** (التقسيم والتجميع)
4. **البند 4** (Soll/Ist) — محتاج عمود DB
5. **البند 3** (شريط المرفقات) — تحسين عرض، ممكن يتأجل

---

---

## ملاحظات لنسخة Java

المواصفة فوق مكتوبة بمنطق مجرّد، بس في ٤ حاجات بتختلف فعليًا لما التنفيذ يكون Java على سيرفر بدل JavaScript في المتصفح. دول مش تفاصيل أسلوب — كل واحدة فيهم بتطلّع أرقام غلط لو اتعملت بالطريقة الحدسية.

### 1. التاريخ تاريخ تقويمي، مش لحظة زمنية

تواريخ المعاملات **`LocalDate`** — يوم في التقويم، مالوش وقت ولا منطقة زمنية.

لو خزّنتها أو قارنتها كـ `Instant` / `ZonedDateTime` / `Timestamp`، فاتورة بتاريخ `2026-07-01` ممكن تقع في **يونيو** لمستخدم في منطقة زمنية ورا UTC. ده بيكسر شاشة مبنية على الشهر بالظبط، وبيحصل في أول وآخر يوم في الشهر بس — يعني هيعدّي من الاختبار ويظهر عند العميل.

> الكود الحالي بيعمل نفس الحماية بطريقة بدائية: بيضيف `T12:00:00` لكل تاريخ قبل ما يحوّله، عشان أي إزاحة زمنية ما تنقلوش ليوم تاني. في Java استخدم `LocalDate` وخلاص.

الأعمدة المناسبة: `date` في Postgres، `LocalDate` في الكود.

### 2. الفلتر الشهري اكتبه كمدى، مش كاستخراج شهر

الشرط المنطقي «نفس السنة ونفس الشهر» لازم يتكتب في الاستعلام كـ **نصف مفتوح**:

```sql
where user_id = ?
  and date >= ?          -- أول يوم في الشهر
  and date <  ?          -- أول يوم في الشهر اللي بعده
```

مش `where extract(month from date) = ?` — دي بتلغي استخدام الـ index وبتخلّي كل استعلام scan كامل.

والـ index المناسب:

```sql
create index if not exists transactions_user_date_idx on transactions(user_id, date);
```

الشاشة الجديدة بتسأل عن **شهر واحد في المرة**، فده أهم index في التطبيق دلوقتي.

### 3. الفلوس `BigDecimal`، وقاعدة التقريب مش اختيارية

الأرقام دي بتروح لبيان UVA بيتقدّم للمالية. `double` مالوش لازمة هنا.

**اشتقاق الصافي من الإجمالي**:

```java
BigDecimal rate   = new BigDecimal("20");                       // نسبة الضريبة
BigDecimal divisor = BigDecimal.ONE.add(rate.movePointLeft(2)); // 1.20
BigDecimal netto  = brutto.divide(divisor, 2, RoundingMode.HALF_UP);
BigDecimal steuer = brutto.subtract(netto);                     // ← مش حساب مستقل
```

**السطر الأخير هو المهم.** لو حسبت `netto` و`steuer` كل واحد لوحده وقرّبت الاتنين، ممكن يطلع `netto + steuer ≠ brutto` بفرق سنت. على مستوى معاملة واحدة ده تافه؛ على مستوى بيان ربع سنوي فيه مئات المعاملات، المجاميع ما بتقفلش والمحاسب بيرجّعلك البيان.

**اشتقاق `steuer` بالطرح بيضمن إن التلاتة بيقفلوا دايمًا.**

نفس القاعدة لما تجمع: اجمع الصافي، اجمع الضريبة، والإجمالي = مجموعهم — ما تقرّبش المجموع تاني.

### 4. التسجيل التلقائي: عندك الحل أصلاً

في نسخة المتصفح، تحويل القواعد المتكررة لمعاملات بيحصل **على الـ client عند كل فتح للتطبيق** — وده اللي بيخلّيه عرضة لـ race مع المزامنة (موصوف في البند 5 فوق).

في نسخة Java على سيرفر، ده مش لازم يكون مشكلة من الأساس: خلّيه **scheduled job** (`@Scheduled` أو أي scheduler عندك) يشتغل مرة في اليوم ويمرّ على الحسابات.

وبرضه حط الحماية على مستوى قاعدة البيانات، لأن الـ job ممكن يشتغل مرتين (restart، أو instance تانية):

```sql
unique (rule_id, period_key)     -- period_key = 'YYYY-MM'
```

مع `insert ... on conflict do nothing`. وقتها التكرار مستحيل مهما حصل.

> سابقة موجودة في الـ schema بتاعنا: `uva_freigaben` فيها `unique (user_id, period_key)` بنفس المنطق.

### ملاحظة على الأسماء

أسماء الحقول في المواصفة (`last_recorded`، `incomeTxId`، `vat_basis`) **وصفية** — مأخوذة من نسخة المتصفح. استخدم تسمياتك أنت؛ اللي يهم هو المعنى:

| في المواصفة | المعنى |
|---|---|
| `last_recorded` | آخر شهر (`YYYY-MM`) اتسجّلت فيه القاعدة دي كمعاملة |
| `incomeTxId` | مرجع المعاملة اللي الفاتورة دي ولّدتها — **وجوده معناه متسجّلة، فما تسجّلهاش تاني** |
| `vat_basis` | `ist` أو `soll` |

## لو احتجت ترجع للتنفيذ الأصلي

الكود اللي اتنفّذ فعليًا موجود في الريبو ده على `main`:

| commit | التغيير |
|---|---|
| `5bd4ceb` | التنقّل الشهري (البند 1) |
| `b8abb0d` | التقسيم والتجميع + الصافي السنوي (البند 2) |
| `78876b1` | شريط المرفقات + إصلاح الازدواج (البند 3 و 5) |
| `138904f` | إعداد Soll/Ist (البند 4) |

مفيد كمرجع لو حاجة في المواصفة مش واضحة — بس **ما تنسخش منه**، الكود مبني على `localStorage` ونسختك مختلفة.
