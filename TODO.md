# SmartAc — Project Roadmap (TODO)

> الحالة: `[x]` = خلص ✅ | `[~]` = جاري / ناقص ⚠️ | `[ ]` = لسه مبدأش ❌

---

## المرحلة 1 — v1.0 (مكتملة ✅)

- [x] تذكير مواعيد الفواتير المستحقة
- [x] WhatsApp share للفواتير (بدل PDF Save)
- [x] Search في المعاملات
- [x] اختبار شامل على موبايل (iOS Safari)
- [x] اختبار شامل على ديسكتوب (Chrome/Firefox)
- [x] رفع v1.0 على Netlify كـ stable version
- [x] commit على GitHub بـ tag "v1.0"

---

## المرحلة 1.5 — Transport Module + Industry Profiles + VAT (مكتملة ✅)

> كل حاجة في نطاق المرحلة دي خلصت. الباقي (AI OCR، Barcode scanner، Restaurant/Construction/Medical) اتأجل عمدًا لـ v2.0 لأنه محتاج Backend أو قرار بايلوت الأول.

### أ) Industry Profiles (الأساس)

- [x] إضافة Business Type selector في الـ Onboarding
- [x] عرض Business Type في Settings (read-only)
- [x] حفظ `state.businessType` في localStorage
- [x] إظهار/إخفاء Fleet في التاب حسب businessType

### ب) VAT System (Brutto/Netto/Steuer)

- [x] إضافة VAT rate field لكل معاملة (0%, 10%, 13%, 20%)
- [x] حساب Brutto/Netto/Steuer أوتوماتيكيًا لكل معاملة (+ معاينة لحظية في المودال + تفاصيل المعاملة)
- [x] عرض Net Income (Income − Expenses − Zahllast) في الـ Home
- [x] تقرير UVA جاهز للمحاسب (PDF/Excel بالألماني، بأرقام الخانات الرسمية KZ 000/022/006/029/060/095)
- [x] نظام Freigabe: اعتماد الفترة (شهري/ربع سنوي/سنوي) + قفل التعديل بعد الاعتماد + ختم "Freigegeben" على التصدير

### ج) UI & UX (مطلوب من الـ pilot)

- ~~تغيير الخلفية/watermark في الصفحات~~ (اتلغت — القرار إن الـ watermark تفضل محذوفة)
- [x] تحسين السلاسة وحركة الـ Modal (قفل تمرير الخلفية على الموبايل + السحب لتحت للإغلاق)
- [x] حركة أنيميشن لعناصر الصفحة الرئيسية عند التحميل (staggered fade-up)
- [x] أشكال متعددة للفواتير (Classic, Modern, Minimal, Transport)
- [x] إعادة تصميم EPC QR Code للدفع البنكي في الفواتير
- [x] Dashboard (Income/Expenses/Overdue/Debts/Net Income)
- [x] إعادة تصميم Bottom Nav + More Menu + Sidebar
- [x] صفحة Help
- [x] إصلاح RTL الكامل للعربي (اتجاه، أسهم، أرقام LTR)

### د) Mitarbeiter (الموظفين الموسع)

- [x] نقل الموظفين من صفحة Fixed لصفحة مستقلة + بروفايل كامل لكل موظف (بيانات شخصية + ملاحظات) — Part 1
- [x] رفع ملفات PDF وصور لكل موظف مع تسمية كل ملف — Part 2 (تخزين محلي IndexedDB، نفس نظام فواتير المصاريف)
- [x] تنبيهات انتهاء الوثائق — Part 2 (أي مستند بتاريخ انتهاء بيظهر في Kalender وبيبعت تنبيه قبل 30 يوم؛ badge أحمر لو منتهي وبرتقالي لو قرب)
- [x] تسجيل ساعات العمل (Arbeitszeiterfassung) — Part 3 (تسجيل يدوي: تاريخ/بداية/نهاية/استراحة/ملاحظة، حساب الساعات تلقائي مع دعم الشِفت الليلي، وملخص إجمالي الشهر)
- [x] التأكد إن حساب الرواتب الشهري التلقائي لسه شغال — لقينا وصلحنا bug: كان بيسجل بس لو المستخدم فاضل مسجل دخول من قبل (auto-login)، مش لو دخل يوزر وباسورد يدوي. اتصلح ودلوقتي بيشتغل في الحالتين

### هـ) Fleet / Fahrzeuge

- [x] صفحة جديدة "Fleet" في التاب (تظهر لأصحاب businessType = transport بس)
- [x] بروفايل لكل سيارة (رقم اللوحة، الاسم/التسمية، النوع، سنة الصنع، ملاحظات)
- [x] رفع ملفات PDF وصور لكل سيارة مع تسمية كل ملف (تخزين محلي IndexedDB، نفس نظام مستندات الموظفين)
- [x] تسجيل الصيانات والمصاريف لكل سيارة (تاريخ، فئة، وصف، تكلفة، إجمالي تلقائي) — سجل توثيقي منفصل عن المعاملات المالية، مش بيتحسب تلقائيًا في الدخل/المصروفات أو UVA
- [x] تنبيهات (Pickerl، TÜV، تأمين، إلخ) — أي مستند بتاريخ انتهاء بيظهر في Kalender وبيبعت تنبيه قبل 30 يوم، زي نظام مستندات الموظفين بالظبط

### و) Kalender & Notizen

- [x] صفحة Kalender مع إضافة موعد
- [x] عرض المواعيد والتنبيهات مع Alarm (reminder)
- [x] صفحة Notizen (ملاحظات حرة: نص + صور + PDF + تكات)
- [x] تنبيهات تلقائية من Mitarbeiter والـ Fleet في Kalender — كل مستند بتاريخ انتهاء (موظف أو عربية) بيظهر أوتوماتيك كموعد في الكالندر + تنبيه قبل 30 يوم

### ز) Anlagevermögen (أصول الشركة)

- [x] صفحة جديدة "Assets" (بتظهر لكل أنواع النشاط، مش بس Transport)
- [x] إضافة أصل (اسم، قيمة، تاريخ الشراء) — أصول عامة (معدات، أثاث...) + العربيات بتظهر تلقائيًا من Fleet من غير تكرار إدخال
- [x] إدخال معدل الإهلاك السنوي (%) لكل أصل — مقترح افتراضي 12.5% (8 سنين) للعربيات، قابل للتعديل من المحاسب
- [x] حساب تلقائي للإهلاك الشهري والقيمة الدفترية الحالية — تقرير بس حاليًا، لسه مش بيتخصم من صافي الدخل في الرئيسية (قرار مؤجل لحد رأي البايلوت)
- [x] تقرير الأصول الكامل (تصدير PDF بالعربيات والأصول العامة + الإجمالي)

### ح) OCR للمصاريف

- [x] فتح الكاميرا وتصوير الفاتورة الورقية (+ رفع صورة/PDF) وإرفاقها بالمصروف — تخزين محلي في IndexedDB
- [x] استخراج Brutto/Netto/MwSt تلقائيًا كاقتراح — OCR محلي (Tesseract.js) والمستخدم يراجع ويأكد
- [ ] استخراج بدقة عالية بالـ AI (Claude API) — Pro feature في v2.0 مع الـ Backend
- [ ] تخزين الصور مجمعة كـ PDF في مجموعات (v2.0)

### ط) إصلاحات وتحسينات جانبية (تمت أثناء بناء المرحلة 1.5 ✅)

- [x] إصلاح bug عزل بيانات المستخدمين (كل يوزر يشوف بياناته بس)
- [x] Notification Center + Toast notifications
- [x] رفع فواتير PDF / تعديل شكل فورم التسجيل
- [x] استبدال كل الإيموجيز في الـ UI بأيقونات SVG احترافية (اللوجو، password strength)
- [x] ربط الموردين/العملاء بجهات الاتصال والديون مع خيار "Other" (Customer/Supplier + Friend/Partner)
- [x] رفع اللوجو الخاص بنا في الفواتير والداشبورد
- [x] Excel Export احترافي (4 sheets: Summary, Transactions, Clients, Suppliers)
- [x] نقل الريبو لـ GitHub Organization (SmartOrdi-OG)

### إضافة المرحلة 1.5 — Retail Module

**Inventory / المخزون (Retail فقط)**

- [x] صفحة جديدة "Inventory" في التاب (بتظهر لأصحاب businessType = retail بس)
- [x] إضافة منتج (اسم، وصف، سعر شراء، سعر بيع، كمية، وحدة القياس)
- [x] تنبيه نفاد المخزون (لما الكمية تقل عن حد معين — قابل للتخصيص لكل منتج، تنبيه فوري + تنبيه يومي لو لسه ناقصة)
- [x] تحديث الكمية عند كل عملية بيع أو شراء — خانة "منتج (اختياري) + كمية" في مودال إضافة الدخل/المصروف؛ الدخل بيخصم من المخزون والمصروف بيضيف عليه
- [x] تقرير المخزون (المنتجات، الكميات، القيمة الإجمالية) — تصدير PDF
- [ ] Barcode scanner (اختياري — v2.0)

**Coming Soon للأنواع التانية (v2.0)**

- [ ] Restaurant / Café → Menu + Tables
- [ ] Construction → Projects + Materials
- [ ] Medical / Clinic → Patients + Appointments

---

### ي) واجهة المحاسب (buchhalter.html) — حل مؤقت محلي قبل الـ Cloud

**ملاحظة:** ده حل مؤقت لحد ما نوصل للمرحلة 2 (Supabase) — شغال بس من نفس المتصفح/الجهاز بتاع صاحب البيزنس (localStorage مش مشترك بين الأجهزة). الـ "Multi-account Viewer" الحقيقي (دخول من أي مكان) موجود تحت في المرحلة 3.

- [x] ملف منفصل `buchhalter.html` — قراءة فقط بالكامل، بدون أي دالة `save()`
- [x] تسجيل دخول بيوزر/باسورد تجريبيين مختلفين عن حسابات المستخدمين (`buchhalter` / `Steuer2026!`)
- [x] لينك "Als Buchhalter anmelden" في صفحة اللوج ان الرئيسية
- [x] اختيار الحساب (قائمة كل حسابات البيزنس المحفوظة في نفس المتصفح)
- [x] تابات: نظرة عامة، UVA، المعاملات، الفواتير، الموظفين، الأصول، تنزيلات — بالألماني بالكامل
- [x] تنزيل UVA كـ PDF، المعاملات كـ CSV، الأصول (Anlagevermögen) كـ PDF، المخزون كـ PDF (للـ retail بس)
- [x] حماية من تعارض IndexedDB مع التطبيق الأساسي (فحص وجود الداتابيز قبل الفتح)

---

## المرحلة 1.5.1 — تعديلات بعد فيدباك البايلوت (مكتملة ✅)

> فيدباك من البايلوت (المحاسب) بعد ما شاف البرنامج شغال فعليًا. التنفيذ هيكون عنصر عنصر بالترتيب، مش كله مرة واحدة.

- [x] **تصحيح لوجيك حساب الضريبة (Netto-based)** — ✅ اتفحص الكود بالكامل (كل مكان بيستخدم vatRate في index.html و buchhalter.html) ولوحظ إن المعادلة أصلاً صح: `Netto = Brutto/(1+rate/100)`, `Steuer = Brutto - Netto` — مطابقة تمامًا لمثال الفاتورة الحقيقي (Brutto 50.02 → Netto 41.68 → Steuer 8.34 عند 20%). الالتباس كان في إدخال البيانات (خانة "المبلغ" اتفهمت غلط إنها Netto بدل Brutto)، مش في الكود. اتأكد بعد ما جرب المستخدم إدخال Brutto زي الفاتورة وطلعت الأرقام مظبوطة. **لا يوجد تعديل كود مطلوب.**

- [x] **واجهة تفصيلية لخانتي المصروفات والدخل (بالـ VAT + Saldo)** — ✅ كارتين "مصروفات" و"دخل" في الصفحة الرئيسية بدل Total Income/Total Expenses القدام (وشيل كارتي الديون المعلقة والفواتير المتأخرة، وكارت Net Income خالص — هيترجع لاحقًا بحسبة مختلفة بعد ما نخلص باقي تعديلات v1.5.1). كل كارت بيعرض إجمالي Netto وإجمالي MwSt للفترة المختارة (اليوم/أسبوع/شهر/سنة/مخصص). الضغط على الكارت الكبير نفسه (مش زرار تحته) بيفتح **جدول مقسّم حسب القسم (Category)** — كل صف قسم (زي Fuel، Supplies، Sales...) بيوضح إجمالي Netto وMwSt بتاعه، وبالضغط عليه بيتوسّع (accordion) يعرض العناصر التفصيلية جواه (الاسم، التاريخ، Netto، MwSt، الإجمالي) مرتبة الأكبر أولاً. الأقسام هي نفس الأقسام الموجودة أصلاً وقت إدخال المعاملة (Category)، وضيف قسم جديد "Fuel/وقود" للمصروفات مخصوص عشان مصاريف البنزين تتفصل عن باقي المستلزمات. خانة نسبة الضريبة في فورم إضافة/تعديل المعاملة بقت خانة رقم يدوية بالكامل (اتشالت أزرار 0/10/13/20% الجاهزة) — تدعم أي نسبة لدعم الشراء من دول تانية.

- [x] **تفصيل خيارات تملك العربيات + نظام "Ausschreibung" (تقسيط مخصص)** — ✅ عند إضافة/تعديل عربية: اختيار طريقة الحصول عليها (شراء كامل / إيجار مع شراء / ليزينج / تقسيط / إيجار فقط)، وسعر العربية Netto + نسبة MwSt منفصلة + تاريخ الشراء (مش ظاهرين لو "إيجار فقط"). خيار "تقسيط السعر كمصروف شهري ثابت (Ausschreibung)": يظهر بس لما طريقة الحصول = شراء كامل، المستخدم يدخل مدة بالشهور، والبرنامج يوزع السعر Netto بالتساوي على المدة دي ويسجله أوتوماتيك كمصروف شهري (قسم Vehicle) لحد ما تخلص المدة. نفس النظام اتعمم كمان على أي أصل عام في صفحة Assets (مش بس العربيات) — أي حاجة سعرها فوق ١٠٠٠ يورو ممكن تتقسط بنفس الطريقة، مربوطة بالأصل بدل العربية. زرار جديد "بيع العربية" في بروفايل العربية: بيدخل سعر وتاريخ البيع، يحسب الفرق بين صافي سعر البيع والرصيد المتبقي من القسط (لو فيه تقسيط شغال)، ويسجل الفرق أوتوماتيك كمكسب (دخل) أو خسارة (مصروف)، وبعدها العربية بتتشال من قايمة الأسطول. **معدل الإهلاك (Depreciation rate) اتلغى بالكامل** من فورمي العربيات والأصول العامة — الكود القديم موجود بس مش مستخدم في أي حسبة، لحد ما تتحسم القصة القانونية/المحاسبية دي مع المحاسب. اتعمل اختبار Playwright شامل للفلو الكامل (تقسيط، تقدم الأقساط، بيع بربح، حذف أصل عام) وكل الاختبارات التانية (14 سكريبت) لسه شغالة تمام.

- [x] **تسجيل ساعات عمل حر بشيت شهري** — ✅ نسخة أولى بمؤشر تنقل بين الشهور + PDF، بعدين اتبدّلت بالكامل بفيدباك من البايلوت ليطابق نموذج ورقي رسمي (Datum/Fahrer/Startzeit/Endzeit/Pause/Gesamtstunden) بعتهولي — شوف التفصيل الكامل تحت "شيت الحضور الشهري الجديد (Grid)".

- [x] **شيت الحضور الشهري الجديد (Grid) — بديل نظام "من تاريخ لتاريخ"** — ✅ بعد فيدباك من البايلوت بصورة نموذج ورقي رسمي، **اتلغى بالكامل** نظام تسجيل الساعات القديم (فورمة واحدة بمدى تاريخ من/إلى). بدل منه: زرار "+ Add Entry" (وكمان زرار تعديل القلم ✏️) بيفتح **شيت شهري كامل** بنفس شكل النموذج الورقي بالظبط — عنوان الشهر بالتسمية النمساوية (زي "Jänner" مش "Januar")، وصف لكل يوم في الشهر المختار (01 لحد آخر يوم) بالترتيب، بأعمدة: Datum (ثابت) / Fahrer (اسم الموظف تلقائي) / Startzeit / Endzeit / Pause (min) / Gesamtstunden — كل خانة قابلة للتعديل بحرية، والساعات بتتحسب أوتوماتيك من الوقت والاستراحة أو تتكتب يدوي. زرار "حفظ" واحد بيحفظ كل الأيام المدخّلة مرة واحدة (يضيف/يعدّل/يمسح حسب البيانات)، ولما تفتح الشيت تاني نفس الشهر بتلاقي كل الأيام اللي دخلتها قبل كده موجودة بقيمها. **PDF Export اتحدّث ليطابق نفس الشكل بالظبط** — كل أيام الشهر (مش بس المدخّلة) بجدول بحدود، + سطر "Unterschrift Fahrer" في الآخر. اتعمل اختبار Playwright شامل للفلو الجديد بالكامل (ملء/حفظ/إعادة فتح وبقاء البيانات/مسح يوم/تصدير PDF)، وكل الاختبارات التانية شغالة تمام.

- [x] **ربط المصروفات الثابتة والمرتبات بخانة المصروفات الجديدة** — ✅ فورمي "إضافة مصروف ثابت" و"إضافة دخل ثابت" بقى فيهم خانة **قسم (Category)** حقيقية (من نفس قوائم الأقسام المستخدمة في المعاملات العادية) وخانة **نسبة ضريبة يدوية اختيارية** (تسيبها 0 لو مفيش). الترحيل الشهري الأوتوماتيك (`checkRecurringDue`) بقى يحمل القسم والضريبة دول للمعاملة المُسجَّلة، فتظهر مجمّعة صح جوا جدول تفصيل المصروفات/الدخل (البند 2). العناصر القديمة اللي كانت موجودة قبل التعديل بترحّل تلقائي بقسم افتراضي "Other" وضريبة 0%. **مرتبات الموظفين (Employees) فضلت زي ما هي بدون ضريبة** — ده صح محاسبيًا مش باگ.

- [x] **تحديث تقارير المحاسب + Excel/PDF export جديد** — ✅ اتعملت مراجعة كاملة لكود التقارير ولوحظت 3 مشاكل حقيقية اتصلحت:
  1. **باگ في حساب الـ Vorsteuer (ضريبة المشتريات)**: الكود القديم كان بيصفّر أي نسبة ضريبة مش 20/13/10% (زي فواتير أجنبية بنسب مخصوصة، اللي بقينا نسمح بيها من البند 2)، يعني خصم ضريبي حقيقي كان بيضيع من تقرير UVA. اتصلح في `computeUva` (index.html) و`computeUvaBu` (buchhalter.html) — دلوقتي Vorsteuer بتتحسب بالنسبة الحقيقية دايمًا (مطابق للواقع إن الفورم الرسمي معندوش تقسيم فئات لضريبة المدخلات أصلاً). جانب المبيعات (Umsätze) لسه مقسّم حسب الفئات الرسمية (KZ022/006/029/معفاة) وأي نسبة غير قياسية بتتجمع في بند إضافي "مبيعات بنسب أخرى" بدل ما تضيع.
  2. **تقرير "Anlagevermögen" في واجهة المحاسب (buchhalter.html) كان لسه بيستخدم موديل الإهلاك القديم بالكامل** (اللي شلناه في البند 3) — اتحدّث ليطابق موديل الاقتناء/التقسيط الجديد (نفس التحديث اللي عملناه في index.html بالبند 3)، في الصفحة نفسها وفي PDF التحميل.
  3. **واجهة المحاسب مكنتش شايفة المصروفات/الدخل الثابت ولا التقسيط النشط خالص** — اتحمّلوا دلوقتي (`recurring`, `fixedIncome`, `installments`) وظهرلهم قسم جديد بالداشبورد (Übersicht) بيوضح الفيكسد كوستس والدخل الثابت (بالقسم والضريبة بتاعتهم) + جدول التقسيطات النشطة (الرصيد المتبقي والتقدم).
  4. **Excel export جديد شامل** بدل الملف اللي كان بس شيت UVA واحد — دلوقتي 4 شيتات في ملف واحد: UVA (ملخص)، Transaktionen (كل المعاملات بالفترة بالـ Netto/MwSt/Steuer/Brutto)، Fixkosten (المصروفات والدخل الثابت + مرتبات الموظفين)، Anlagevermögen (الأصول والتقسيط النشط).
  UVA PDF وباقي التصديرات فضلت زي ما هي شكليًا، بس بعد إصلاح باگ الـ Vorsteuer. اتعمل اختبار Playwright جديد شامل للفلو ده وكل الـ 18 اختبار (القديمة + الجديدة) شغالة تمام.

- [x] **تحسينات إضافية على صفحة Reports وشيت الإكسيل بتاع UVA** — ✅ بعد فيدباك تاني من البايلوت على شكل صفحة Reports الفعلي:
  1. **كارت "This Month/Quarter..." في صفحة Reports**: اتشال "Net Profit" و"Profit Margin" و"Fixed Income" (كان بيسبب ازدواج في الحساب — بيتحسب مرة كمعاملة حقيقية مرحلة أوتوماتيك ومرة تانية كإسقاط شهري منفصل). Total Income وTotal Expenses بقى تحتهم مباشرة سطر صغير بيوضح Net وVAT (بنفس حسبة الداشبورد `calculateNetIncome`)، فالأرقام بقت متطابقة تمامًا مع الصفحة الرئيسية ومفيش تكرار.
  2. **كارت Forecast اتنقل لآخر صفحة Reports** (بعد كارت UVA)، من غير أي تعديل في محتواه — مش أولوية دلوقتي بس المستخدم ممكن يوصله لو عايز.
  3. **شيت الإكسيل بتاع UVA (زرار "Excel" في كارت UVA) اتبنى من الأول بالكامل** ليطابق شكل ملف "Einnahmen-Ausgaben-Rechnung" الرسمي اللي المحاسب باعته (نموذج نمساوي معروف): شيت **"Übersicht"** (ملخص Netto/MwSt/Brutto للدخل والمصروفات + جدول شهري مختصر لكل شهر Netto/MwSt/Brutto) + **شيت لكل شهر** فعليًا داخل الفترة المختارة بس (لو الفترة شهر واحد → شيت واحد، ربع سنة → 3 شيتات، سنة كاملة → 12 شيت)، كل شيت شهري فيه ملخص Netto/MwSt/Brutto للدخل والمصروفات + جدول معاملات (تاريخ/وصف/عمودين Netto+MwSt+إجمالي للدخل وعمودين للمصروفات) وعدد الصفوف بيزيد وينقص حسب عدد المعاملات الفعلي (مش عدد ثابت). شيتي Fixkosten وAnlagevermögen (من البند اللي فات) فضلوا مضافين في الآخر كمعلومات إضافية للمحاسب.
  اتعمل اختبار Playwright جديد شامل وكل الـ 20 اختبار شغالة تمام.

- [x] **تفصيل Vorsteuer حسب النسبة (توضيحي)** — ✅ المستخدم استفسر ليه معاملة بضريبة 19% مش ظاهرة منفصلة في UVA؛ اتأكد إن الحساب صح (Vorsteuer رقم إجمالي واحد في الفورم الرسمي، مفيهوش تقسيم حسب النسبة أصلاً)، وطلب زود عرض توضيحي عشان يشوف كل حاجة. اتضاف تحت صف "KZ 060 — Vorsteuer" مباشرة قائمة صغيرة بعنوان "Breakdown by rate (for your reference — not an official form field)" بتوضح مساهمة كل نسبة ضريبة على حدة (20%، 19%، أي نسبة تانية) في إجمالي الـ Vorsteuer — للتوضيح جوا التطبيق بس، مش جزء من الفورم الرسمي ولا داخلة في تصديرات PDF/Excel المُوجَّهة للمحاسب (اللي فضلت مطابقة للفورم الرسمي بالظبط). اتعمل اختبار Playwright جديد وكل الاختبارات شغالة تمام.

- [x] **إصلاح شيت ساعات العمل الشهري (باگ PDF + صيغة الساعات) + إلغاء كل الإيموجي من التطبيق** — ✅ بعد فيدباك تاني من البايلوت:
  1. **باگ حقيقي في PDF ساعات العمل**: صف عناوين الأعمدة كان بيطلع صناديق سودة من غير نص (ما عدا أول عمود). السبب: مكتبة jsPDF بتستخدم نفس الـ "fill color" الداخلية لكل من رسم المربعات المملوءة (`rect(...,'F')`) وكتابة النص (`text()`) — فلما بيترسم مربع ملون وبعده نص، النص بياخد لون التعبئة ويسيبه أسود، وبعدين أي مربع تاني بيترسم بياخد نفس اللون الأسود ده. الإصلاح: إعادة تحديد لون التعبئة/النص قبل كل خلية على حدة بدل مرة واحدة قبل الحلقة. اتأكد الإصلاح بمقارنة رندر فعلي للـ PDF قبل وبعد.
  2. **إجمالي الساعات بقى بصيغة ساعة:دقيقة** (زي "9:44") بدل الكسر العشري ("9.73") — في الشيت الشهري، في PDF، وفي القائمة المختصرة تحت.
  3. **إلغاء كل الإيموجي من التطبيق بالكامل** — كان فيه حوالي 280 إيموجي في 39 نوع مختلف موزعين على كل الصفحات (أيقونات المعاملات، أزرار المشاركة/الإيميل/واتساب، عناوين الأقسام، رسائل الإشعارات، الأسهم، علامات الصح/الفشل...إلخ). اتستبدلوا كلهم بأيقونات SVG من نفس مكتبة الأيقونات المستخدمة أصلاً في التطبيق (أو اتشالوا لو كانوا زخرفة بحتة زي 🚀). النصوص اللي بتتبعت لبرة (رسائل واتساب/إيميل، إشعارات النظام) اتشال منها الإيموجي بدون بديل (مينفعش SVG في نص عادي). أيقونة المعاملات في القوائم بقت مشتقة من قسم المعاملة (Category) بدل ما تتخزن كإيموجي ثابت وقت الإضافة — أدق وأسهل للصيانة.

- [x] **تكملة Export Backup + مزامنة واجهة المحاسب مع كل تعديلات v1.5.1** — ✅ المستخدم سأل عن فتح واجهة المحاسب من جهاز تاني بدون سيرفر، واتوضّح إن الفلو الحالي (نفس المتصفح، اختيار الحساب من القايمة) هيفضل زي ما هو تمامًا. طلب بدل كده يتصلح `exportBackup()`/`handleImportBackup()` (index.html) عشان يشيلوا/يرجّعوا كل حقول البيزنس (كانت ناقصة: `fleet`, `assets`, `inventory`, `businessName`, `businessType`, `bizTaxId`, `uvaFreigaben`)، ويتأكد إن واجهة المحاسب بتعرض التعديلات الأخيرة صح. اتعمل:
  1. **`exportBackup`/`handleImportBackup` بقوا شاملين كل حاجة** — الأسطول، الأصول العامة، المخزون، اسم ونوع النشاط، حالات اعتماد UVA (Freigaben) بقوا جوا ملف النسخة الاحتياطية.
  2. **واجهة المحاسب اتحدّثت لتطابق التصميم الجديد لساعات العمل** — تاب Mitarbeiter بقى بيعرض إجمالي الساعات بصيغة ساعة:دقيقة ("9:14") زي التطبيق الرئيسي، بدل الكسر العشري القديم.
  3. **تفصيل Vorsteuer حسب النسبة اتضاف لتاب UVA في واجهة المحاسب كمان** — نفس العرض التوضيحي اللي في التطبيق الرئيسي (مش جزء من PDF/Excel).
  4. **باقي الإيموجي المتبقية في `buchhalter.html`** (السهم في زرار الرجوع، علامة الصح في بادج Freigegeben، أيقونات التنزيل، زرار الإغلاق ✕) اتحولت لـ SVG زي باقي التطبيق.
  اتعمل اختبار Playwright كامل: تصدير نسخة احتياطية حقيقية من التطبيق الرئيسي والتأكد إن كل الحقول الجديدة موجودة، وفتح واجهة المحاسب بنفس المتصفح والتأكد إن كل التابات (Übersicht، UVA، Mitarbeiter، Anlagevermögen، Downloads) بتعرض البيانات صح بالشكل الجديد ومفيش إيموجي متبقي، وإن كل أزرار تنزيل الـ PDF لسه شغالة.

- [x] **الفيو الافتراضي للصفحة الرئيسية بقى "الشهر" بدل "اليوم" + إضافة تبويب "الربع"** — ✅ المستخدم طلب إن أول ما تتفتح الصفحة الرئيسية يبقى الفلتر واقف على الشهر مش اليوم، مع إبقاء إمكانية التنقل الحر بين كل الفترات. `state.period` الافتراضي اتغيّر من `'today'` لـ `'month'`، وتبويب "Month" بقى هو الـ active افتراضيًا في الـ HTML الأساسي. كمان اتضاف تبويب **"Quarter/الربع/Quartal"** جديد جنب باقي الفترات (كان ناقص أصلاً من فلاتر الصفحة الرئيسية، موجود بس في صفحة Reports وUVA) — بمنطق فلترة كامل (`getFiltered`, `getPrevPeriodData`, `getPrevPeriodLabel`) وحساب المصروفات/الدخل الثابت الشهري مضروب في 3 لما الفترة ربع سنوي. اتعمل اختبار Playwright يتأكد إن الفتح الأول للتطبيق بيبدأ بـ"Month" فعّال، وإن التبديل لكل الفترات (اليوم/الأسبوع/الشهر/الربع/السنة) شغال، وإن إعادة تحميل الصفحة بترجع تاني للشهر كديفولت، وإن الترجمة العربية/الألمانية للتبويب الجديد ظاهرة صح.

- [x] **إصلاح دياُلوج التأكيد على الحذف (باگ z-index شامل) + إصلاح تقدم الأقساط (Ratenfortschritt) للتواريخ الماضية** — ✅ فيدباك من البايلوت بصورتين: دياُلوج تأكيد حذف تسجيل ساعات عمل ظاهر "برة" مش فوق الشاشة اللي بيحذف منها، وعربية اتسجل تاريخ شرائها من شهرين فات وبرضه شايف "0/24" مش "2/24".
  1. **باگ z-index حقيقي وشامل في `confirmAsync`/`showConfirm`** (الدياُلوج العام المستخدم في **كل** رسائل التأكيد في التطبيق — 19 استخدام مختلف): `.confirm-overlay` كان `z-index:9000`، أوطى من أي أوفرلاي صفحة تفصيلية زي بروفايل الموظف (9999) أو شيت ساعات العمل (100000). يعني أي تأكيد حذف بيتفتح وانت جوا صفحة تفصيلية كان بيترسم **تحت** الصفحة دي (مخفي تمامًا)، ولحد ما تقفل الصفحة وترجع للقائمة الرئيسية كان الدياُلوج يفضل مفتوح وينكشف فجأة فوق صفحة تانية خالص — بالظبط اللي حصل في الصورة. الإصلاح: رفع `z-index` لـ `.confirm-overlay` لـ 1000000 (أعلى من أي أوفرلاي تاني في التطبيق)، فدلوقتي أي تأكيد حذف بيظهر فورًا فوق نفس الشاشة اللي انت فيها، أيًا كانت.
  2. **تقدم الأقساط (Ausschreibung) كان بيتصفر عند الإنشاء بتاريخ شراء في الماضي**: `checkInstallmentsDue()` كان بيسجل شهر واحد بس في كل مرة يفتح التطبيق فيها (وبس لو الشهر الحالي لسه ما اتسجلش)، من غير ما "يلحق" الشهور اللي فاتت فعليًا لو تاريخ الشراء قديم أو التطبيق ما اتفتحش لمدة. الإصلاح: بقى بيحسب عدد الشهور الفعلي اللي عدى من تاريخ الشراء لحد النهاردة (`monthsBetween`) وبيسجّل كل معاملة مصروف ناقصة دفعة واحدة (بتاريخ أول كل شهر ناقص) لحد ما يلحق بالعدد الصح، بدل ما يقف عند شهر واحد بس. كمان بقى بيتنادى فورًا لحظة إنشاء أي قسط جديد (عربية أو أصل عام) بدل ما ينتظر أول تحميل تاني للصفحة. اتعمل اختبار Playwright: عربية بتاريخ شراء من شهرين، اتأكد إن `monthsPosted` بيبقى 2 فورًا (مش 0) مع تسجيل المعاملتين الناقصتين بتواريخهم الصح، وإن صفحة Assets بتعرض "2/24" صح.

- [x] **إصلاح شكل حقول التاريخ (باگ "طبقة فوق بعض" على iOS) + صفحة "كل المعاملات" جديدة + بحث بالتاريخ/الشهر** — ✅ فيدباك من البايلوت بصورتين: شكل حقول التاريخ حاسس إنه كذا طبقة فوق بعض على الآيفون، وزرار "عرض الكل" في المعاملات كان بيوديه لصفحة التقارير مش لقائمة معاملات فعلية.
  1. **باگ `color-scheme` في كل حقول `<input type="date">`** — على iOS/Safari، الحقل الأصلي (نص + أيقونة التقويم) بيترسم بألوان الوضع الفاتح الافتراضية بتاعت النظام مهما كانت خلفية التطبيق، وده بيظهر كـ"صندوق جوا صندوق" مش متناسق. الإصلاح: قاعدة CSS مركزية واحدة على كل حقول التاريخ (`color-scheme` تتبع ثيم التطبيق فاتح/غامق، `text-align:center` عشان التاريخ يتكتب في النص، و`appearance:none`) بدل ما كل حقل يكون له ستايل مبعثر، فبقى شكلها موحّد في كل أماكن التطبيق دفعة واحدة.
  2. **زرار "عرض الكل" (See all) بقى بيفتح صفحة "كل المعاملات" فعلية** بدل ما يوديك لصفحة التقارير — بتعرض كل المعاملات (دخل ومصروف) حسب الفترة المختارة حاليًا في الصفحة الرئيسية (يوم/أسبوع/شهر/ربع/سنة/فترة مخصصة)، مع خانة بحث ونفس تصميم قائمة المعاملات المعتاد.
  3. **البحث (في الصفحة الرئيسية وفي صفحة "كل المعاملات" الجديدة) بقى بيدور بالتاريخ والشهر مش بس بالاسم/القسم** — كتابة تاريخ بأي صيغة (زي "2026-07-12") أو اسم شهر بلغة الواجهة الحالية (زي "July"/"يوليو"/"Juli") بيفلتر المعاملات المطابقة، بالإضافة لما كان موجود قبل كده (الاسم، القسم، اسم العميل/المورد). اتعمل اختبار Playwright شامل: فتح صفحة كل المعاملات من "عرض الكل" (مش صفحة التقارير)، التأكد إنها بتحترم الفترة المختارة، البحث بتاريخ كامل وباسم شهر بالإنجليزي والألماني، وزرار الرجوع بيقفل الصفحة صح.

- [x] **إلغاء زرار "Export to Excel" العام من كارت التقارير الرئيسي** — ✅ المستخدم طلب إلغاؤه لأنه بقى مالوش لزمة. اتشال الزرار من كارت "This Month/Year..." في صفحة Reports مع كل الكود المرتبط بيه (المودال، دوال `openExcelExportModal`/`exportToExcel`/`generateExcelFile` وباقي الدوال المساعدة، الترجمات، وذكره في صفحة المساعدة/FAQ) — كان كود مستقل تمامًا عن Excel export بتاع كارت UVA (`uvaExportExcel`, الشيت الرسمي Einnahmen-Ausgaben-Rechnung اللي بيتبعت للمحاسب) فمفيش أي تأثير عليه، اتأكد إنه لسه شغال 100% بعد الحذف. زراري Share وPDF في نفس الكارت فضلوا زي ما هم من غير تغيير.

- [x] **إصلاح باگ z-index شامل تاني: شاشات التعديل/الإضافة بتظهر "برة المكان"** — ✅ المستخدم لاحظ إن فتح تعديل معاملة من صفحة "كل المعاملات" الجديدة كانت الشاشة بتظهر في مكان غلط، وطلب فحص المشكلة دي في كل حاجة فيها تعديل أو إضافة بالتطبيق (نفس فئة باگ الـ z-index اللي اتصلحت قبل كده في دياُلوج التأكيد، بس هنا في مكان تاني). السبب: `.modal-overlay` — الكلاس المشترك اللي بتستخدمه **كل** فورمات الإضافة/التعديل في التطبيق (المعاملة الرئيسية، موظف، عربية، صيانة، أصل، منتج، موعد، مستندات — 11+ فورم) — كان `z-index:200`، أوطى بكتير من أي صفحة بروفايل/قائمة في التطبيق (2000 لحد 100000). يعني أي فورم إضافة/تعديل بيتفتح وانت جوا أي صفحة تفصيلية كان بيترسم **تحتها** (مخفي)، بالظبط زي باگ دياُلوج التأكيد اللي اتصلح قبل كده. نفس المشكلة كانت موجودة في `#txDetailOverlay` (تفاصيل/تعديل المعاملة)، `.edit-debt-overlay`، `.export-overlay`، `.policy-overlay`، `.client-profile-overlay`، `#invShareModal`، و`.cleardata-overlay` — كلهم أوطى من صفحات البروفايل. الإصلاح: رفع كل الكلاسات/العناصر دي لـ `z-index:500000` (تدرج جديد فوق أي صفحة بروفايل في التطبيق، وتحت دياُلوج التأكيد 1000000). بما إن `.modal-overlay` كلاس مشترك، إصلاح واحد غطى كل الـ 11+ فورم دفعة واحدة. اتعمل اختبار Playwright لسيناريو المستخدم بالظبط (فتح كل المعاملات → تفاصيل → تعديل) وسيناريو تاني (فتح فورم إضافة من جوا بروفايل موظف) — الاتنين اتأكد إن شاشة التعديل/الإضافة بتظهر فوق الصفحة الحالية صح.

- [x] **فيتشر جديد: أرشيف المستندات (فولدرات + ملفات)** — ✅ صاحب البيزنس يقدر يحفظ كل المستندات المهمة (أوراق تسجيل الشركة، عقود...إلخ) منظمة في فولدرات. نقطة الدخول: زرار "الأرشيف" جديد في قائمة "المزيد" (وفي الـ sidebar لسطح المكتب) تحت قسم "أدوات". الفلو: صفحة الأرشيف بتعرض قايمة الفولدرات (اسم + عدد المستندات) مع زرار "+ فولدر جديد"؛ الضغط على فولدر يفتح صفحة تفصيلية بيها زرار "+ إضافة مستند" (كاميرا أو رفع ملف PDF/صورة، مع اسم وملاحظة اختيارية لكل مستند)، وكل مستند فيه تعديل (الاسم/الملاحظة) وحذف (بتأكيد)، والضغط عليه بيفتح نفس الـ Viewer المستخدم في باقي التطبيق لعرض المستندات؛ الفولدر نفسه فيه إعادة تسمية وحذف (حذف الفولدر بيمسح كل المستندات جواه بعد تأكيد بيوضح العدد). البيانات: `state.archiveFolders`/`state.archiveDocs` (metadata) + IndexedDB منفصلة (`smartac_archive`) للملفات نفسها، بنفس نمط تخزين مستندات الموظفين المستخدم أصلاً في التطبيق (ضغط الصور، نفس الـ Viewer، نفس أسلوب حفظ/حذف). اتعمل اختبار Playwright شامل للفلو الكامل (إنشاء فولدر → رفع مستند PDF مع ملاحظة → عرضه → تعديله → إعادة تسمية الفولدر → حذف المستند → حذف الفولدر) وكل شغال 100%، بالإضافة لفحص التصميم بالعربي (RTL) والألماني.

- [x] **إصلاح: زرار إغلاق الـ Viewer (X) مش شغال — عيب جانبي من إصلاح z-index اللي فات** — ✅ المستخدم اشتكى إنه لازم يرجع ويعمل Refresh عشان يقفل عرض أي ملف بدل ما يستخدم زرار X. السبب: لما رفعنا `z-index` بتاع `#txDetailOverlay` لـ `500000` في الإصلاح السابق (عشان فورمات التعديل تبقى فوق صفحات التفاصيل)، نسينا إن `openReceiptViewer`/`openInvoice` (عارض الفواتير والمستندات نفسه) لسه على `99999` — يعني بقى **أوطى** من صفحة تفاصيل المعاملة اللي بيتفتح منها، فبقى بيترسم تحتها (مخفي بالكامل بما فيه زرار الـ X)! نفس المشكلة اتلاقت في `showToast()` (رسائل التنبيه/الأخطاء، زي "من فضلك اكتب مبلغ صحيح") و`#inAppNotifContainer` (تنبيهات الفواتير/الاشتراكات) — كانوا برضه أوطى من الفورمات (`z-index:99999`/`100000` مقابل `500000`). الإصلاح: رفع الأربعة عناصر دول لـ `z-index:600000` (فوق أي فورم/صفحة، وتحت دياُلوج التأكيد بس 1000000) — كده أي Viewer أو تنبيه هيفضل ظاهر وقابل للإغلاق أيًا كان السياق اللي اتفتح منه. اتعمل اختبار Playwright لنفس السيناريو بالظبط اللي بلّغ عنه المستخدم (فتح فاتورة/مستند من جوا تفاصيل المعاملة) وتأكد إن زرار الـ X شغال فعليًا (ضغطة فأرة حقيقية على الإحداثيات، مش استدعاء JS مباشر) وإنه بيرجع لتفاصيل المعاملة زي ما هي من غير أي Refresh.
- [x] **فيتشر جديد: مخزن قطع الغيار/الزيوت لأصحاب نشاط النقل (Transport)** — ✅ البايلوت محتاج يعرف كمية قطع الغيار/الزيوت اللي عنده ويسجل الاستهلاك فيقل العدد تلقائيًا. اتعمل بإعادة استخدام صفحة "المخزون" الموجودة أصلاً لأصحاب Retail بدل عمل صفحة جديدة من الصفر:
  - اتفتحت صفحة المخزون (القائمة + السايدبار + صفحة المساعدة + ربط منتج بمعاملة الشراء) لأصحاب نشاط Transport بالظبط زي Retail (كانت متقفلة بشرط `businessType === 'retail'` بس في 6 أماكن، ولقيت مانع سابع مهم جوا `showNav()` نفسها كانت بتمنع الدخول للصفحة أصلاً لغير Retail حتى لو القائمة ظاهرة).
  - **التزويد (زيادة الكمية)**: بيستخدم نفس آلية Retail الموجودة — تسجيل مصروف (Ausgabe) وربطه بصنف من المخزن، فتزيد الكمية أوتوماتيك.
  - **الاستهلاك (فيتشر جديد بالكامل)**: زرار "استهلاك" جنب كل صنف، بيفتح فورم: الكمية المستهلكة (مع تحقق إنها مش أكبر من الكمية المتاحة) + اختيار العربية اللي اتستخدم فيها (اختياري، من قائمة الأسطول) + ملاحظة (اختياري). بيقلل الكمية مباشرة **من غير** إنشاء أي معاملة مالية (لأنه مش بيع ولا شراء)، ومع سجل "آخر عمليات الاستهلاك" بيتخزن جوا الصنف نفسه (`product.usageLog`) ويتعرض في نفس الفورم.
  - اتعمل اختبار Playwright كامل: تفعيل نشاط Transport + إضافة عربية + إضافة صنف (زيت موتور) + محاولة استهلاك كمية أكبر من المتاح (رفض صحيح) + استهلاك كمية صحيحة مربوطة بعربية وملاحظة (نجح، الكمية اتقللت، مفيش معاملة مالية اتعملت، السجل ظهر صح لما فتحت الفورم تاني) + تأكيد إن الصفحة لسه شغالة لأصحاب Retail زي ما هي (مفيش Regression). اتشغل باقي سويت الاختبارات (smoke, fleet, reports, archive) وكلها Pass.
  - **تعديل بعد فيدباك المستخدم**: أول نسخة كانت ورّثت مفاهيم Retail البيعية (سعر بيع، ربط صنف بمعاملة دخل) اللي مالهاش معنى لمخزن مستلزمات داخلية (البايلوت مش بيبيع الزيت/القطع، بس بيستخدمها). التعديل لأصحاب Transport تحديدًا: (1) خانة "سعر البيع" اتشالت من فورم الإضافة/التعديل خالص، وقيمتها بتتسجل صفر تلقائيًا، (2) شكل الصنف في القائمة بقى يعرض الكمية بس (مش سعر بيع)، (3) ربط صنف بمعاملة اتقصر على معاملات المصروف (تزويد) بس، واختفى من معاملات الدخل خالص (مبيعش يترتبط ببيع)، وتغيّر اسم الخانة لـ"تزويد صنف من المخزن" بدل "المنتج" عشان يبقى أوضح. حسابات Retail لسه شغالة زي ما هي بالظبط (سعر البيع وربط الدخل موجودين زي الأول). اتعمل اختبار Playwright يتأكد من الفرق بين الاتنين واتشغلت باقي السويت وكلها Pass.
- [x] **فيتشر جديد: Fahrtenbuch (دفتر يومية السيارة) في بروفايل كل عربية** — ✅ المستخدم بعت نموذج Fahrtenbuch رسمي (PDF) وطلب سجل بنفس الشكل بالظبط يتملى ويتعدل فيه بحرية. أول نسخة كانت جدول واحد مستمر بزرار "+ إضافة صف"، بس المستخدم صحح الطلب: عايزها **بالظبط زي فورمة ساعات عمل الموظفين (Arbeitszeiterfassung) الموجودة أصلاً** — يختار شهر، ويلاقي كل أيام الشهر ده جاهزة كصفوف جدول يملاها بحرية. اتعمل إعادة بناء كاملة على نفس نمط ساعات العمل بالظبط:
  - في بروفايل كل عربية: قسم "Fahrtenbuch" فيه تنقل بين الشهور (أسهم يمين/شمال) + ملخص الشهر (إجمالي كم/خاص/خدمة) + قائمة مختصرة بالأيام اللي فيها بيانات بس (زي قائمة ساعات العمل بالظبط) + زرار "+ Add Trip" بيفتح شاشة الشهر الكاملة.
  - شاشة الشهر (`fbOpenMonthSheet`) بترسم **صف جاهز لكل يوم من أيام الشهر** (زي `whDaysInMonth` بالظبط، نفس الدالة المستخدمة في ساعات العمل)، بنفس أعمدة النموذج الرسمي: وقت/مكان المغادرة، عداد الكيلومترات (بداية)، وقت/مكان الوصول، عداد الكيلومترات (نهاية)، الكيلومترات المقطوعة، الغرض من الرحلة، العميل، مدة الرحلة، خاص كم، خدمة كم — والتاريخ نفسه ثابت لكل صف مش خانة قابلة للتعديل (زي التاريخ في شيت ساعات العمل بالظبط). عنوان الشاشة بيستخدم اسم الشهر الرسمي بالألماني النمساوي (`whAtMonthLabel`، "Juli 2026") زي مستند رسمي حقيقي.
  - "الكيلومترات المقطوعة" بتتحسب أوتوماتيك (عداد الوصول − عداد المغادرة) لما تتغير خانة عداد المغادرة أو الوصول، لكن تفضل خانة عادية قابلة للتعديل اليدوي بحرية كاملة.
  - الحفظ يبقى دفعة واحدة (زرار "Save" في الأعلى) لكل أيام الشهر مرة واحدة — بالظبط زي حفظ شيت ساعات العمل: أي يوم اتسيب فاضي بالكامل بيتشال من `v.trips` تلقائيًا لو كان له سجل قديم، وأي يوم فيه بيانات بيتسجل/يتحدّث. البيانات بتتخزن جوا كل عربية نفسها (`v.trips`) زي `v.maintenance` بالظبط، مش Array منفصل في الـ state.
  - زرار "Export PDF" بينتج ملف PDF بنفس تخطيط النموذج الرسمي بالظبط (Landscape) لشهر واحد بس (الشهر المعروض حاليًا، زي مفهوم "Seite" في النموذج الأصلي)، بهيدر فيه رقم اللوحة واسم الشهر واسم الشركة، وصف Summe في الآخر.
  - النمط الجديد ده اتجنب باگ الـ DOM اللي ظهر في أول نسخة (إعادة رسم الجدول من جوا حدث `onchange` بتاع خلية فيه نفسه) خالص، لأن الحفظ بقى دفعة واحدة بزرار مش لايف على كل تعديل خلية — بالظبط زي ساعات العمل.
  - اتعمل اختبار Playwright كامل من الصفر يغطي النمط الجديد: فتح بروفايل عربية + تأكيد إن شاشة الشهر بترسم بالظبط عدد أيام الشهر (٣١ يوم ليوليو) + تعبئة يوم واحد وتأكيد الحساب التلقائي للكيلومترات + حفظ دفعة واحدة + تأكيد ظهور السجل في القائمة المختصرة + التنقل بين الشهور (الشهر الجاي فاضي، والرجوع للشهر الأصلي يورّي السجل تاني) + إعادة فتح شاشة الشهر والتأكد إن اليوم اتعبى فيه البيانات القديمة صح + حذف السجل من القائمة المختصرة + تصدير PDF من غير أخطاء. اتشغلت باقي سويت الاختبارات (smoke, fleet, reports, archive, inventory) وكلها Pass.
- [x] **فيتشر جديد: اختيار شكل الفاتورة (Classic/Modern) مع معاينة توضيحية** — ✅ المستخدم طلب أشكال فواتير متعددة يختار بينها، مع صورة توضيحية لكل شكل عشان ياخد فكرة قبل ما يختار. اتعمل:
  - قسم جديد "شكل الفاتورة" في الإعدادات (بعد قسم الحساب البنكي)، فيه بطاقتين قابلتين للاختيار: "Classic" (التصميم الأخضر الأصلي بتاع التطبيق) و"Modern" (تصميم جديد بشريط علوي بلون التطبيق الأساسي `#1a7fd4`، تخطيط أبسط بدون حدود شبكة كاملة، صندوق إجمالي مدوّر).
  - كل بطاقة فيها **معاينة توضيحية مصغّرة (Mockup)** — مجرد خطوط ومربعات CSS بسيطة برضه نفس ألوان وتخطيط التصميم الفعلي (مش رندر حقيقي لفاتورة ببيانات)، عشان تكون خفيفة وسريعة الظهور. البطاقة المختارة بتتحدد بإطار وخلفية بلون Primary + علامة صح.
  - الاختيار بيتخزن في `state.invoiceTemplate` (فيلد كان موجود بالفعل من قبل بس مش متفعّل/مستخدم في أي حتة)، ويأثر بس على الفواتير الصادرة والواردة (مش الإيصالات، اللي ليها تصميمها الخاص من قبل).
  - **اكتشاف مهم أثناء التنفيذ**: لقيت 3 دوال بناء فواتير كاملة (`buildModernInvoice` قديمة، `buildMinimalInvoice`، `buildTransportInvoice`) موجودة بالفعل جوا الكود من محاولة سابقة مالهاش أي استدعاء من أي مكان خالص (Dead code تمامًا، سكافولدينج قديم اتسيب من غير ما يتوصل). كمان كانت هتعمل تعارض حقيقي (Duplicate function name) مع التصميم الجديد اللي بنيته. اتشالت الثلاثة دوال دي بالكامل والاستبدال بتصميم Modern جديد متكامل مبني على نفس نمط Classic (يدعم كل بيانات العمل، الملاحظات، طريقة الدفع، تفاصيل البنك + QR).
  - اتعمل اختبار Playwright: التأكد من ظهور القسم + الاختيار الافتراضي Classic + الضغط على Modern بيحدّث `state.invoiceTemplate` ويحفظ + توليد HTML الفاتورة بالتصميمين من غير أخطاء (بيحتوي على اللون المميز لكل تصميم) + استمرارية الاختيار بعد إعادة تحميل الصفحة. اتشغلت باقي سويت الاختبارات وكلها Pass.
- [x] **أساس فيتشر مسح الفواتير بالجملة + AI (لحد ما نضيف الـ API الحقيقي)** — ✅ المستخدم طلب زرار "مسح/رفع فواتير" يسمح بتصوير أو رفع كذا فاتورة مرة واحدة (مصروف أو دخل)، يستخرج منها التاريخ والـ Netto/Brutto/الضريبة، ويسجلهم كمعاملات منظمة بالفترة الزمنية عشان المحاسب. اتعمل الأساس الكامل (واجهة + تدفق) بحيث لما نضيف API حقيقي للـ AI يبقى استبدال نقطة واحدة بس:
  - زرار "مسح الفواتير" جديد في قائمة More تحت "أدوات"، وكمان زرار سريع منفصل (Camera icon) فوق زرار الـ "+" الرئيسي في الصفحة الرئيسية مباشرة (بعد ما المستخدم طلب يكون متاح من غير ما يدخل قائمة More) — بيفتح نفس المسار مباشرة، من غير ما يأثر على سلوك زرار "+" الأصلي خالص.
  - **خطوة ١**: اختيار النوع (مصروف/دخل) — مودال بسيط بزرارين.
  - **خطوة ٢**: شاشة كاملة لإضافة الفواتير — كاميرا (تصوير كذا صورة ورا بعض) أو رفع (كذا ملف صورة/PDF مرة واحدة)، مع قائمة بالملفات المضافة وإمكانية حذف أي واحد قبل التحليل.
  - **خطوة ٣**: تحليل تسلسلي لكل ملف (مؤشر تقدّم "X / Y") عن طريق دالة معزولة واحدة `extractInvoiceData(file)` — دلوقتي بتستخدم نفس آلية الـ OCR المحلي (Tesseract) الموجودة أصلاً لإيصال المعاملة الفردية، وزودت عليها استخراج التاريخ (regex على صيغ التاريخ الشائعة) اللي مكانش موجود قبل كده. **ده هو نقطة الاستبدال الوحيدة لما نضيف الـ API الحقيقي** — نفس شكل النتيجة `{date, brutto, rate}` هيفضل زي ما هو، فمفيش أي تعديل مطلوب في باقي الواجهة.
  - **خطوة ٤**: شاشة مراجعة إجبارية قبل الحفظ — كل فاتورة في Card منفصل، فيه معاينة + التاريخ والـ Brutto ونسبة الضريبة (كلهم قابلين للتعديل اليدوي)، مع Netto محسوب أوتوماتيك وعرض تحذير "معرفناش نقراها" لو الاستخراج فشل (زي حالة PDF أو صورة مش واضحة). ده ضروري لأن الاستخراج الحالي heuristic ومش مضمون ١٠٠٪.
  - **الحفظ**: بيعمل معاملة منفصلة لكل فاتورة بالنوع المختار، الفئة "Other"، والتاريخ اللي اتراجع، ومرفق بيها ملف الفاتورة الأصلي (زي أي إيصال عادي). التصنيف بالفترة الزمنية بيتحقق تلقائيًا لأن كل معاملة بتاريخها الصحيح، ونظام التقارير/UVA الموجود بيصنف حسب التاريخ زي أي معاملة تانية — مفيش داعي لأي بنية بيانات جديدة.
  - اتعمل اختبار Playwright كامل: الدخول من القائمة + اختيار مصروف + رفع صورة وملف PDF مع بعض + التحليل (مع تأكد إن فشل الـ OCR — زي حالة عدم الاتصال بالإنترنت — بيتعامل معاه بسلاسة وبيوري رسالة "معرفناش نقراها" بدل ما يعلّق) + تعديل يدوي للأرقام في المراجعة وتأكد إن الـ Netto بيتحسب صح لحظيًا + الحفظ بينشئ معاملتين صح بالنوع والفئة الصحيحين ومرفق بيهم الملفات + الرجوع للصفحة الرئيسية. اتشغلت باقي سويت الاختبارات وكلها Pass.

---

## أفكار مؤجلة (اتقالت، لسه مش هنبدأ فيها)

- [x] **لينكات لمواقع حكومية لحساب Brutto/Netto لمرتبات الموظفين** — ✅ اتضاف قسم جديد "روابط مفيدة/Useful Links" في صفحة Help (بعد قسم "الفريق والأصول")، فيه أول رابط: "BMF Brutto-Netto-Rechner" (`https://onlinerechner.haude.at/BMF-Brutto-Netto-Rechner/`) — حاسبة صافي الراتب الرسمية النمساوية. بيفتح في تاب/نافذة متصفح جديدة تمامًا (`window.open(..., '_blank', 'noopener')`) من غير ما يأثر على حالة التطبيق، وله أيقونة "External Link" مميزة عن باقي صفوف الـ Help العادية (اللي بتفتح تفاصيل جوا التطبيق نفسه). اتعمل اختبار Playwright يتأكد إن الضغطة فعلاً بتفتح تاب/صفحة جديدة حقيقية (`ctx.waitForEvent('page')`) مش مجرد تنقل داخل نفس الصفحة، وإن صفحة الـ Help الأصلية تفضل زي ما هي بعد كده.
- [x] **لينك لموقع حكومي يحسب أيام عمل الموظف السنوية والدخل الناتج عنه** — ✅ اتضاف رابط تاني في نفس قسم "روابط مفيدة" بصفحة Help: "Sozialversicherung Tarifrechner" (`https://www.sozialversicherung.at/trechner/views/tarifrechner.xhtml`) — حاسبة التأمين الاجتماعي/التعرفة الرسمية النمساوية. نفس الآلية بالظبط زي رابط الـ BMF (فتح في تاب متصفح جديد، أيقونة External Link مميزة).

---

## Push Notifications (بعد التحويل للـ Cloud/Backend)

- [ ] Service Worker setup للـ PWA
- [ ] طلب إذن الـ notifications من المستخدم
- [ ] Push notifications للمواعيد والتنبيهات
- [ ] Push notifications لانتهاء وثائق الموظفين
- [ ] Push notifications لمواعيد الـ Fleet (Pickerl, TÜV)
- [ ] Push notifications للفواتير المتأخرة
- [ ] دعم iOS (PWA Add to Home Screen — من iOS 16.4+)
- [ ] دعم Android Chrome كامل

---

## المرحلة 2 — Supabase Setup

- [x] إنشاء project على supabase.com — ✅ أول محاولة كانت بمنطقة London بالغلط، اتلغت واتعمل project جديد بمنطقة **Frankfurt (eu-central-1)** للـ DSGVO compliance (London برة الاتحاد الأوروبي رسميًا بعد Brexit، وحتى لو فيه Adequacy Decision حاليًا، مش نفس ضمان إن البيانات تفضل جوا الـ EU فعليًا).
- [x] إنشاء الـ tables — ✅ سكيما كاملة (21 جدول) في `supabase/migrations/20260712120000_initial_schema.sql`، اتأكد وجودها كلها فعليًا بـ query مباشر على `information_schema.tables`. الجداول: `profiles` (إعدادات البيزنس)، `transactions`، `debts`، `clients`، `suppliers`، `employees` + `employee_work_hours` + `employee_documents`، `recurring`، `fixed_income`، `installments`، `issued_invoices`، `received_invoices`، `fleet` + `fleet_maintenance`، `assets`، `inventory`، `notes`، `appointments`، `uva_freigaben`، `receipts` — كل جدول مربوط بـ `user_id` بيرجع لـ `auth.users`. اتعمل تحقق محلي (Postgres مؤقت على الجهاز، من غير أي لمسة لداتابيز Supabase الحقيقية) للتأكد إن كل الـ SQL شغال صح قبل الرفع.
- [x] نشر تلقائي حقيقي للـ migrations — ✅ الـ GitHub App integration بتاعة Supabase (اللي جوا الداشبورد) اتضح إنها مش بتنشر أوتوماتيك عند الـ push (فضلت واقفة على "No migrations" رغم محاولتين). بدالها اتعمل **GitHub Actions workflow** مستقل (`.github/workflows/supabase-migrations.yml`) بيثبّت Supabase CLI ويعمل `supabase link` + `supabase db push` أوتوماتيك على أي push لـ `main` بيغيّر حاجة جوا `supabase/migrations/`، مستخدم secrets (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`) مخزّنة في GitHub نفسه (محدش شافهم غير GitHub). اتعمل الملف idempotent بالكامل (كل `create policy` بقى مسبوق بـ `drop policy if exists`) عشان يفضل شغال حتى لو اتنفذ أكتر من مرة. اتأكد الـ workflow شغال فعليًا: أول تشغيل نجح خلال 17 ثانية، وصفحة الـ project بقت توضح "Last Migration: initial_schema" بدل "No migrations".
- [x] ضبط Row Level Security (RLS) — ✅ كل الجداول الـ 21 عليها RLS مفعّل، وكل واحد فيهم عنده policy بسيط: كل يوزر يشوف/يعدّل بياناته هو بس (`auth.uid() = user_id`)، بالإضافة لـ 2 policy على مستوى Supabase Storage (bucket الفواتير `receipts` وbucket مستندات الموظفين `employee-documents`) بنفس المبدأ.
- [x] اختيار EU region (Frankfurt) للـ DSGVO compliance — ✅ (جزء من إنشاء الـ project)
- [x] Trigger أوتوماتيك — ✅ لما يوزر جديد يعمل حساب (signup)، صف `profiles` بتاعه بيتعمل تلقائيًا.
- [x] تفعيل Supabase Auth (email + password) — ✅ اتأكد إنه مفعّل (شغال افتراضيًا في أي مشروع جديد). ملاحظة: التطبيق الحالي (`index.html`) لسه بيستخدم نظام تسجيل الدخول بتاعه الخاص (`btoa` + localStorage)، الاستبدال بـ Supabase Auth مهمة في المرحلة 3.
- [x] اختبار API (insert + select) — ✅ اختبار end-to-end حقيقي للـ RLS باستخدام يوزر تجريبي حقيقي (اتعمل عن طريق Authentication → Add User، مش insert مباشر في `auth.users`) ومحاكاة جلسة تسجيل دخول فعلية (`request.jwt.claim.sub` + `set local role authenticated`) في SQL Editor: (1) اليوزر بيضيف معاملة ويشوفها — نجح (2) يوزر تاني (uid عشوائي) — مايشفهاش خالص (3) زائر مش مسجل دخول (`role anon`) — مايشفهاش خالص. الثلاث حالات طلعوا مظبوطين تمامًا، يعني الـ RLS بتحمي البيانات فعليًا مش بس نظريًا.
- [ ] تصميم نظام الـ Viewer/Accountant access في الـ database

---

## المرحلة 3 — تحويل الـ App للـ Cloud (v2.0)

- [ ] إنشاء ملف smartac_v2.html كنسخة جديدة
- [ ] استبدال الـ auth (btoa → Supabase Auth)
- [ ] تحويل loadUserData() من localStorage لـ Supabase
- [ ] تحويل save() من localStorage لـ Supabase
- [ ] تحويل transactions CRUD
- [ ] تحويل clients CRUD
- [ ] تحويل suppliers CRUD
- [ ] تحويل invoices CRUD
- [ ] تحويل recurring/fixed/employees CRUD
- [ ] تحويل debts CRUD
- [ ] تحويل fleet/assets/notes/appointments CRUD
- [ ] رفع الملفات (أرشيف/إيصالات/فواتير/مستندات موظفين) فعليًا على Supabase Storage بدل IndexedDB المحلي — عشان أي ملف يترفع من جهاز يبقى متاح ومتنزّل من أي جهاز تاني بنفس الحساب (Buckets `receipts` و `employee-documents` جاهزين من المرحلة 2، محتاجين ربط فعلي + bucket جديد للأرشيف)
- [ ] إضافة loading states على كل العمليات
- [ ] إضافة error handling ورسائل خطأ للمستخدم
- [ ] اختبار multi-device (موبايل + ديسكتوب بنفس الأكاونت)
- [ ] اختبار offline behavior

### Multi-account Viewer (للمحاسب)

- [ ] نظام طلب الـ Access من صاحب الأكاونت
- [ ] Viewer mode (قراءة فقط، بدون تعديل)
- [ ] قائمة بالأكاونتات اللي عنده access عليها + دخول بضغطة واحدة على الاسم

### Chat مع المحاسب

- [ ] Chat بين صاحب البزنس والمحاسب (Supabase Realtime)
- [ ] إرسال ملفات وصور في الـ chat
- [ ] إشعارات رسايل جديدة

### Gmail Integration

- [ ] Gmail OAuth login
- [ ] كتابة وإرسال إيميلات مع إرفاق ملفات
- [ ] قراءة الإيميلات من جوا البرنامج
- [ ] ربط الإيميلات بالعملاء والموردين

---

## المرحلة 4 — Freemium + Payment

- [ ] تطبيق حدود الـ Free plan في الكود
- [ ] عمل Upgrade Modal
- [ ] إعداد Stripe account احترافي
- [ ] إعداد Stripe Payment Link (€2.99/شهر)
- [ ] كتابة Supabase Edge Function تستقبل Stripe webhook
- [ ] تغيير الـ plan في الـ database أوتوماتيكيًا بعد الدفع
- [ ] إرسال إيميل تأكيد الاشتراك (Resend)
- [ ] اختبار flow كامل: دفع → plan يتغير → features تتفتح
- [ ] OCR Invoice Scanning كـ Pro feature (Claude API من الـ backend)
- [ ] Puppeteer PDF Generation كـ Pro feature (لضمان تطابق شكل الـ PDF مع الـ View)
- [ ] AI Invoice Data Extraction كـ Pro feature

---

## المرحلة 5 — Launch

- [ ] تحديث Privacy Policy لـ cloud version
- [ ] Verarbeitungsverzeichnis (سجل المعالجة الداخلي)
- [ ] Landing page احترافية على smartordiog.eu
- [ ] إعداد custom domain على Netlify
- [ ] أول TikTok/YouTube Short
- [ ] نشر عربي عن البرنامج في Facebook Groups لأصحاب المشاريع الصغيرة
- [ ] جمع أول 50 مستخدم وتسجيل الـ feedback
- [ ] تحديد pricing نهائي بناءً على الـ feedback

---

## ملاحظات

- **v1.0** = localStorage only — stable وجاهزة للتجربة
- **v1.5** = Transport Pilot (Innsbruck) + VAT System — مكتملة ✅
- **v1.5.1** = تعديلات بعد فيدباك البايلوت (تصحيح حساب الضريبة + واجهة المصروفات/الدخل + العربيات + ساعات العمل + التقارير) — مكتملة ✅
- **v2.0** = Cloud version — الـ production الحقيقي
- لا تبدأ المرحلة التالية قبل ما تتأكد إن المرحلة الحالية خلصت
- OCR + PDF Generation (Puppeteer) + AI Extraction = Pro features مدفوعة في v2.0
- Gmail Integration + Multi-account + Chat = v2.0 فقط (محتاجين backend)
- DATEV ASCII Export = مرحلة مستقبلية للسوق النمساوي/الألماني
- حد الـ 10 معاملات في addTransaction() (leftover من v1.0) اتشال مؤقتًا — البيزنس متاح بلا حدود لحد ما نبني نظام الـ Free plan الحقيقي في المرحلة 4
- **⚠️ Vercel Auto-Deploy توقف لما الـ Repo بقى Private**: لما اتعمل الـ GitHub repo Private، الـ GitHub↔Vercel integration وقف يشتغل تمامًا (Push على main مبقاش بيعمل Deploy جديد خالص، من غير أي خطأ ظاهر). السبب: خطة Vercel الحالية (Hobby) محتاجة إما الريبو يفضل Public أو Upgrade لـ Pro عشان يقدر يشتغل مع Private repos. الحل المؤقت: الريبو رجع Public تاني والـ Deploy رجع يشتغل عادي. **لو حد فكر يخلي الريبو Private تاني في المستقبل، لازم Upgrade لـ Vercel Pro الأول وإلا الـ Auto-Deploy هيوقف تاني.**
